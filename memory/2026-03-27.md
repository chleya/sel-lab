# 2026-03-27

- Continued the Phase 3 selector-line infrastructure work instead of returning to local hybrid mechanisms.
- Added shared stress-benchmark helpers in `analysis/phase3_benchmark_family.py`:
  - unified stress-family config construction
  - shared policy-summary aggregation
  - shared benchmark-family runner
- Rewired the main stress-family scripts onto that shared layer:
  - `analysis/phase3_diagnostic_suite.py`
  - `analysis/phase3_diagnostic_benchmark_family.py`
  - `analysis/phase3_regime_switch_benchmark.py`
  - `analysis/phase3_diagnostic_selector_benchmark.py`
  - `analysis/phase3_fingerprint_selector_benchmark.py`
  - `analysis/phase3_signature_selector_benchmark.py`
  - `analysis/phase3_fitted_selector_benchmark.py`
- Fixed the direct `python tests\smoke_test.py` entrypoint so fixture-backed tests no longer fail spuriously in `main()`.
- Added benchmark-family smoke coverage:
  - shared stress-benchmark helper now has a smoke test path
  - smoke now also checks registry resolution for selector gate families
- Extended `core/phase3_registry.py` with formal selector-gate benchmarks:
  - `selector_generalization_gate` -> `feature_shift_nonnegative`
  - `selector_adversarial_gate` -> `feature_shift_embedded`
  - family `selector_gate_map`
  - family `selector_full_map`
- Added `analysis/phase3_selector_stress_map_benchmark.py`:
  - formal selector stress map that combines:
    - plasticity stress
    - interference stress
    - stronger interference stress
    - retrieval ambiguity stress
    - selector generalization gate
    - selector adversarial gate
- Interpretation:
  - selector exploration is no longer just a few separate reports
  - the project now has a formal registry-backed map for screening future learned selectors against both the main family and the adversarial gate
  - the next meaningful step is to run a new learned selector candidate on `selector_full_map`, not to add more local conflict-aware policy tweaks
- Verification:
  - `python -c "import analysis.phase3_benchmark_family, analysis.phase3_signature_selector_benchmark, tests.smoke_test; print('imports-ok')"`
  - `python -c "from analysis.phase3_selector_stress_map_benchmark import run_benchmark; from core.phase3_registry import resolve_phase3_diagnostic_family; print(resolve_phase3_diagnostic_family('selector_full_map'))"`
  - `python tests\smoke_test.py`
- Added a new learned-selector candidate:
  - policy: `task_specialist_clone_prototype_selector_merge`
  - mechanism: nearest-exemplar selector over standardized task-signature features
  - training family: `expanded_stress_map`
  - evaluation family: `selector_full_map`
  - benchmark:
    - `analysis/phase3_prototype_selector_benchmark.py`
    - `PHASE3_PROTOTYPE_SELECTOR_BENCHMARK.md`
    - `results/canonical/phase3_prototype_selector_benchmark.json`
- Also fixed a real infrastructure bug revealed by that run:
  - `feature_shift_embedded` requires `input_size=64`
  - shared `build_stress_benchmark_config(...)` now routes that suite through the high-dimensional config path instead of assuming the default 4-D feature-shift shape
- Prototype-selector result:
  - on the formal family and selector-full-map stresses, `prototype_selector` matches the same regime choice as `fingerprint_selector`
  - it does not improve the adversarial gate
  - on `selector_adversarial_gate`:
    - `prototype_selector`: mean `-1.7%` vs fixed
    - `fingerprint_selector`: mean `-1.7%` vs fixed
    - `signature_selector`: mean `+0.0%` vs fixed
    - `regime_switch`: mean `-0.6%` vs fixed
- Interpretation:
  - exemplar-style nonlinearity over the current feature set is still not enough
  - the bottleneck is no longer just "linear selector surface too weak"
  - richer supervision or richer regime features are still required
- Verification:
  - `python analysis\phase3_prototype_selector_benchmark.py`
  - `python tests\smoke_test.py`
- Extended the selector feature pipeline from static task signatures to richer online regime features:
  - added task-level learning-dynamics traces inside `ReusePolicyNetwork`
  - new online regime features:
    - `loss_mean`
    - `loss_delta`
    - `conflict_delta`
    - `confidence_delta`
  - preserved the old six-feature signature path for the earlier selector baselines
- Added a new learned selector candidate:
  - policy: `task_specialist_clone_dynamics_selector_merge`
  - implementation: linear fitted selector over the richer ten-feature regime vector
  - benchmark:
    - `analysis/phase3_dynamics_selector_benchmark.py`
    - `PHASE3_DYNAMICS_SELECTOR_BENCHMARK.md`
    - `results/canonical/phase3_dynamics_selector_benchmark.json`
- Dynamics-selector result:
  - aggregate on `selector_full_map`:
    - `dynamics_selector`: mean `+4.0%` vs `adapt_only`, mean `+5.0%` vs fixed
    - this effectively ties `fingerprint_selector` on the full map aggregate
  - but it does not improve the selector gates that matter:
    - `selector_adversarial_gate` (`feature_shift_embedded`): `-1.7%` vs fixed
    - `selector_sparse_adversarial_gate` (`feature_shift_sparse_embedded`): `-1.0%` vs fixed
  - compared baselines:
    - `signature_selector` still remains better on the embedded adversarial gate at `+0.0%` vs fixed
    - hardcoded `regime_switch` remains stronger on the embedded adversarial gate at `-0.6%` vs fixed
- Interpretation:
  - richer task-dynamics features are not useless, but in the current linear fitted setup they mostly recover the same selector surface as the existing fingerprint route
  - the main bottleneck now looks even less like "missing online dynamics" and more like supervision/objective mismatch or the need for a more structured selector target
- Report chain synced:
  - `analysis/generate_unified_report.py` manifest now includes `phase3_dynamics_selector_benchmark.json`
  - regenerated `UNIFIED_REPORT.md`
- Verification:
  - `python analysis\phase3_dynamics_selector_benchmark.py`
  - `python analysis\generate_unified_report.py`
  - `python tests\smoke_test.py`
- Tried stronger selector supervision instead of another feature family:
  - new policy: `task_specialist_clone_outcome_selector_merge`
  - benchmark:
    - `analysis/phase3_outcome_selector_benchmark.py`
    - `PHASE3_OUTCOME_SELECTOR_BENCHMARK.md`
    - `results/canonical/phase3_outcome_selector_benchmark.json`
  - supervision target:
    - fit the selector against the real outcome margin between `task_specialist_clone_limited_merge` and `task_specialist_clone_current_path_boost_merge`
    - not a hardcoded family label
    - not a handpicked regime rule
- Outcome-selector result:
  - aggregate on `selector_full_map`:
    - `outcome_selector`: mean `+2.8%` vs `adapt_only`, mean `+3.8%` vs fixed
  - this is weaker than:
    - `fingerprint_selector`: mean `+4.0%` vs `adapt_only`, mean `+5.0%` vs fixed
    - `signature_selector`: mean `+3.9%` vs `adapt_only`, mean `+4.8%` vs fixed
  - on selector gates:
    - `selector_adversarial_gate` (`feature_shift_embedded`): `-0.6%` vs fixed
    - `selector_sparse_adversarial_gate` (`feature_shift_sparse_embedded`): `-2.4%` vs fixed
  - interpretation of that pattern:
    - result-driven supervision is informative, but this simple linear outcome fit over the current regime features still overcommits to the `limited_merge` side
    - it partially recovers the main embedded adversarial gate relative to `fingerprint_selector` / `dynamics_selector`
    - but it is worse than `signature_selector` on the embedded gate and much worse on the sparse adversarial gate
- Current selector read is now sharper:
  - static task fingerprints can recover the formal family
  - richer dynamics features alone do not beat that
  - outcome-supervised linear fitting does not solve the gate problem either
  - next selector work should likely move to a more structured target/objective, not just another linear fit
- Verification:
  - `python analysis\phase3_outcome_selector_benchmark.py`
  - `python tests\smoke_test.py`
- Moved from plain outcome supervision to structured selector objectives over the same regime features.
- Added a suite-level ranking selector:
  - policy: `task_specialist_clone_ranking_selector_merge`
  - benchmark:
    - `analysis/phase3_ranking_selector_benchmark.py`
    - `PHASE3_RANKING_SELECTOR_BENCHMARK.md`
    - `results/canonical/phase3_ranking_selector_benchmark.json`
  - result:
    - ranking-style supervision is better aligned than the simple outcome fit
    - aggregate improves to about `+4.6%` vs fixed on `selector_full_map`
    - but `feature_shift_embedded` still remains behind `signature_selector`
- Raised the structured objective to task-level ranking:
  - policy: `task_specialist_clone_task_ranking_selector_merge`
  - benchmark:
    - `analysis/phase3_task_ranking_selector_benchmark.py`
    - `PHASE3_TASK_RANKING_SELECTOR_BENCHMARK.md`
    - `results/canonical/phase3_task_ranking_selector_benchmark.json`
  - result:
    - aggregate becomes the strongest single-head learned selector on `selector_full_map`
    - mean `+4.2%` vs `adapt_only`
    - mean `+5.1%` vs fixed
    - `feature_shift_embedded` recovers to `+0.0%` vs fixed, matching `signature_selector`
    - but `feature_shift_sparse_embedded` degrades to roughly `-2.2%` vs fixed
  - interpretation:
    - the core gain comes from target/objective structure, not just more online features
    - but sparse embedded ambiguity and embedded ambiguity still do not share the same best single linear boundary
- Tried a constrained version of task-level ranking:
  - policy: `task_specialist_clone_constrained_task_ranking_selector_merge`
  - benchmark:
    - `analysis/phase3_constrained_task_ranking_selector_benchmark.py`
    - `PHASE3_CONSTRAINED_TASK_RANKING_SELECTOR_BENCHMARK.md`
    - `results/canonical/phase3_constrained_task_ranking_selector_benchmark.json`
  - result:
    - sparse-gate-aware weighting does not fix the single-head failure mode
    - embedded gate stays at `+0.0%` vs fixed
    - sparse embedded gate still sits around `-2.2%` vs fixed
  - interpretation:
    - the bottleneck is no longer just the objective
    - a single global score surface is itself the problem
- Shifted from single-head ranking to segmented routing:
  - policy: `task_specialist_clone_two_stage_selector_merge`
  - benchmark:
    - `analysis/phase3_two_stage_selector_benchmark.py`
    - `PHASE3_TWO_STAGE_SELECTOR_BENCHMARK.md`
    - `results/canonical/phase3_two_stage_selector_benchmark.json`
  - mechanism:
    - embedded-like tasks route through the `signature_selector`-style branch
    - sparse embedded-like tasks explicitly fall back to the boost-side branch
    - remaining tasks use the task-ranking selector fallback
  - result:
    - current best learned selector on `selector_full_map`
    - mean `+4.4%` vs `adapt_only`
    - mean `+5.3%` vs fixed
    - `feature_shift_embedded`: `+0.0%` vs fixed
    - `feature_shift_sparse_embedded`: `-0.8%` vs fixed
    - this beats:
      - `task_specialist_clone_task_ranking_selector_merge` on aggregate
      - `task_specialist_clone_fingerprint_selector_merge` on sparse embedded gate
      - `task_specialist_clone_signature_selector_merge` on aggregate
- Current selector read after the ranking chain:
  - richer online features alone were not enough
  - plain outcome-supervised fitting was not enough
  - structured ranking was necessary to improve the learned-selector frontier
  - but the decisive step was changing selector form from one boundary to two-stage routing
  - next selector work should start from the two-stage router and try to learn the routing split more cleanly, not return to another single-head fit
- Tried replacing the hand-tuned two-stage split with a learned three-way router over the same regime features:
  - policy: `task_specialist_clone_learned_router_selector_merge`
  - benchmark:
    - `analysis/phase3_learned_router_selector_benchmark.py`
    - `PHASE3_LEARNED_ROUTER_SELECTOR_BENCHMARK.md`
    - `results/canonical/phase3_learned_router_selector_benchmark.json`
  - mechanism:
    - fit a three-class linear router over task-level regime features
    - route classes:
      - `plasticity_boost`
      - `interference_control`
      - `sparse_plasticity_boost`
    - evaluation still uses the learned task-ranking payload as the fallback baseline for fair comparison
  - result:
    - it does not beat the current two-stage frontier
    - aggregate lands at `+4.8%` vs fixed
    - this is roughly back at the `signature_selector` level, below:
      - `task_specialist_clone_two_stage_selector_merge`: `+5.3%` vs fixed
      - `task_specialist_clone_task_ranking_selector_merge`: `+5.1%` vs fixed
    - key gate reads:
      - `feature_shift_embedded`: `-0.8%` vs fixed
      - `feature_shift_sparse_embedded`: `-1.5%` vs fixed
  - interpretation:
    - simply changing the two-stage thresholds into a flat learned multiclass router is not enough
    - the current hand-designed split is still carrying signal that the linear learned router does not recover from the existing feature/label setup
    - next work should not be "another linear router"; it should target better routing targets, calibration sweeps, or a more expressive segmented selector form
- Ran a focused local calibration sweep around the current two-stage sparse-route thresholds:
  - script:
    - `analysis/phase3_two_stage_selector_sweep.py`
    - `PHASE3_TWO_STAGE_SELECTOR_SWEEP.md`
    - `results/canonical/phase3_two_stage_selector_sweep.json`
  - sweep neighborhood:
    - current default `(zero_ratio_floor=0.385, conflict_delta_floor=-0.005)`
    - nearby zero-ratio alternatives `0.365`, `0.405`
    - nearby conflict-delta alternatives `-0.015`, `0.005`
  - read:
    - the current default remains the best local point in the focused sweep
    - moving `zero_ratio_floor` upward to `0.405` clearly hurts sparse embedded performance
    - moving `zero_ratio_floor` downward to `0.365` slightly hurts the main embedded gate without helping sparse embedded
    - nudging `conflict_delta_floor` to `-0.015` is effectively identical to the default
    - nudging it to `0.005` slightly hurts sparse embedded
  - interpretation:
    - there is no obvious local threshold-tuning free win left inside the current two-stage form
    - the remaining gain is unlikely to come from just retuning the current sparse-route cutoff pair
    - next selector work should shift from threshold calibration to better routing targets or a more expressive segmented router
- Tried a hierarchical segmented selector instead of a flat learned router:
  - policy: `task_specialist_clone_hierarchical_selector_merge`
  - benchmark:
    - `analysis/phase3_hierarchical_selector_benchmark.py`
    - `PHASE3_HIERARCHICAL_SELECTOR_BENCHMARK.md`
    - `results/canonical/phase3_hierarchical_selector_benchmark.json`
  - mechanism:
    - keep the existing segmentation into:
      - `default`
      - `embedded`
      - `sparse_embedded`
    - fit a separate ranking head per region over the same regime features
    - benchmark implementation was corrected once during the run so baseline selectors also receive their trained payloads during comparison
  - fair result after rerun:
    - aggregate on `selector_full_map`:
      - `hierarchical_selector`: mean `+4.2%` vs `adapt_only`, mean `+5.1%` vs fixed
      - `two_stage_selector`: mean `+4.4%` vs `adapt_only`, mean `+5.3%` vs fixed
      - `task_ranking_selector`: mean `+4.2%` vs `adapt_only`, mean `+5.1%` vs fixed
    - key gates:
      - `selector_adversarial_gate` (`feature_shift_embedded`):
        - `hierarchical_selector`: `-0.2%` vs fixed
        - `two_stage_selector`: `+0.0%` vs fixed
      - `selector_sparse_adversarial_gate` (`feature_shift_sparse_embedded`):
        - `hierarchical_selector`: `-1.6%` vs fixed
        - `two_stage_selector`: `-0.8%` vs fixed
  - interpretation:
    - segmented routing alone was not the remaining missing ingredient; simply replacing the fallback with region-specific linear heads does not beat the current hand-designed two-stage frontier
    - this does beat the flat learned router on aggregate and on the main embedded gate
    - but it still loses to `two_stage_selector`, especially on the sparse embedded adversarial gate
    - next selector work should not just be "more linear heads behind the same segmentation"; it likely needs better routing targets, richer region-specific supervision, or a genuinely more expressive segmented model
- Tried a more expressive segmented selector instead of stopping at linear region heads:
  - policy: `task_specialist_clone_hierarchical_quadratic_selector_merge`
  - benchmark:
    - `analysis/phase3_hierarchical_quadratic_selector_benchmark.py`
    - `PHASE3_HIERARCHICAL_QUADRATIC_SELECTOR_BENCHMARK.md`
    - `results/canonical/phase3_hierarchical_quadratic_selector_benchmark.json`
  - mechanism:
    - keep the same `default` / `embedded` / `sparse_embedded` segmentation
    - replace each region head with a quadratic ranking surface over regime features
    - reuse fair baseline payload injection for `task_ranking` and `two_stage` comparisons
  - result:
    - aggregate on `selector_full_map`:
      - `hierarchical_quadratic_selector`: mean `+4.1%` vs `adapt_only`, mean `+5.0%` vs fixed
      - `two_stage_selector`: mean `+4.4%` vs `adapt_only`, mean `+5.3%` vs fixed
      - `task_ranking_selector`: mean `+4.2%` vs `adapt_only`, mean `+5.1%` vs fixed
    - key gates:
      - `selector_adversarial_gate` (`feature_shift_embedded`):
        - `hierarchical_quadratic_selector`: `-0.5%` vs fixed
        - `two_stage_selector`: `+0.0%` vs fixed
      - `selector_sparse_adversarial_gate` (`feature_shift_sparse_embedded`):
        - `hierarchical_quadratic_selector`: `-2.0%` vs fixed
        - `two_stage_selector`: `-0.8%` vs fixed
  - interpretation:
    - a more expressive quadratic head does improve over the linear hierarchical variant
    - but it still does not recover the gate-preserving behavior of the current two-stage selector
    - this makes the bottleneck look even less like "head expressivity within a fixed segmentation" and more like routing-target quality or the need for explicit sparse-gate protection inside the segmented objective itself
- Tested that last hypothesis directly by adding explicit sparse-gate protection into the segmented objective:
  - policy: `task_specialist_clone_hierarchical_sparse_gate_selector_merge`
  - benchmark:
    - `analysis/phase3_hierarchical_sparse_gate_selector_benchmark.py`
    - `PHASE3_HIERARCHICAL_SPARSE_GATE_SELECTOR_BENCHMARK.md`
    - `results/canonical/phase3_hierarchical_sparse_gate_selector_benchmark.json`
  - mechanism:
    - keep the same segmented quadratic head form
    - strengthen the sparse-region supervision target and pairwise weighting on `selector_sparse_adversarial_gate`
    - keep a smaller protective upweight on the embedded adversarial gate
  - result:
    - aggregate on `selector_full_map`:
      - `hierarchical_sparse_gate_selector`: mean `+4.1%` vs `adapt_only`, mean `+5.0%` vs fixed
      - `two_stage_selector`: mean `+4.4%` vs `adapt_only`, mean `+5.3%` vs fixed
      - `task_ranking_selector`: mean `+4.2%` vs `adapt_only`, mean `+5.1%` vs fixed
    - key gates:
      - `selector_adversarial_gate` (`feature_shift_embedded`):
        - `hierarchical_sparse_gate_selector`: `-0.5%` vs fixed
        - `two_stage_selector`: `+0.0%` vs fixed
      - `selector_sparse_adversarial_gate` (`feature_shift_sparse_embedded`):
        - `hierarchical_sparse_gate_selector`: `-2.0%` vs fixed
        - `two_stage_selector`: `-0.8%` vs fixed
  - interpretation:
    - simply baking sparse-gate-aware weighting into the current segmented quadratic objective does not rescue the frontier
    - the result lands back near the single-head task-ranking aggregate instead of the stronger two-stage aggregate
    - this makes the remaining bottleneck look even more like routing-target mismatch or the need to preserve an explicit hard sparse branch, not just stronger weights inside the current segmented fitting recipe
- Tested the harder preservation hypothesis directly:
  - policy: `task_specialist_clone_guarded_router_selector_merge`
  - benchmark:
    - `analysis/phase3_guarded_router_selector_benchmark.py`
    - `PHASE3_GUARDED_ROUTER_SELECTOR_BENCHMARK.md`
    - `results/canonical/phase3_guarded_router_selector_benchmark.json`
  - mechanism:
    - keep the hard sparse branch from `two_stage_selector`
    - do not learn the `sparse_embedded` region at all
    - only fit quadratic heads for `default` and `embedded`
  - result:
    - aggregate on `selector_full_map`:
      - `guarded_router_selector`: mean `+4.0%` vs `adapt_only`, mean `+5.0%` vs fixed
      - `two_stage_selector`: mean `+4.4%` vs `adapt_only`, mean `+5.3%` vs fixed
    - key gates:
      - `selector_adversarial_gate` (`feature_shift_embedded`):
        - `guarded_router_selector`: `-0.2%` vs fixed
        - `two_stage_selector`: `+0.0%` vs fixed
      - `selector_sparse_adversarial_gate` (`feature_shift_sparse_embedded`):
        - `guarded_router_selector`: `-1.2%` vs fixed
        - `two_stage_selector`: `-0.8%` vs fixed
  - interpretation:
    - preserving the hard sparse branch helps relative to the sparse-weighted segmented fit
    - but it still does not recover the full `two_stage_selector` frontier
    - this makes the remaining gap look even more like the exact hand-designed embedded branch logic in `two_stage_selector`, not just the presence of a hard sparse branch by itself
- Tested the strongest preservation hypothesis in this selector family:
  - policy: `task_specialist_clone_guarded_expert_selector_merge`
  - benchmark:
    - `analysis/phase3_guarded_expert_selector_benchmark.py`
    - `PHASE3_GUARDED_EXPERT_SELECTOR_BENCHMARK.md`
    - `results/canonical/phase3_guarded_expert_selector_benchmark.json`
  - mechanism:
    - keep the hard sparse branch from `two_stage_selector`
    - keep the embedded signature branch from `two_stage_selector`
    - learn only a quadratic head for the remaining `default` region
  - result:
    - aggregate on `selector_full_map`:
      - `guarded_expert_selector`: mean `+4.3%` vs `adapt_only`, mean `+5.3%` vs fixed
      - `two_stage_selector`: mean `+4.4%` vs `adapt_only`, mean `+5.3%` vs fixed
    - key gates:
      - `selector_adversarial_gate` (`feature_shift_embedded`):
        - `guarded_expert_selector`: `+0.0%` vs fixed
        - `two_stage_selector`: `+0.0%` vs fixed
      - `selector_sparse_adversarial_gate` (`feature_shift_sparse_embedded`):
        - `guarded_expert_selector`: `-0.8%` vs fixed
        - `two_stage_selector`: `-0.8%` vs fixed
  - interpretation:
    - this closes essentially all of the meaningful gap to `two_stage_selector`
    - the remaining aggregate difference is a tiny tail effect on non-gate benchmarks, not a failure on the main adversarial gates
    - the selector story is now much sharper: the durable advantage comes from preserving both the hard sparse branch and the embedded signature branch, while the learned default-region fit is good enough
- Ran the direct simplification check after `guarded_expert`:
  - benchmark:
    - `analysis/phase3_guarded_expert_simplification_benchmark.py`
    - `PHASE3_GUARDED_EXPERT_SIMPLIFICATION_BENCHMARK.md`
    - `results/canonical/phase3_guarded_expert_simplification_benchmark.json`
  - tested policies:
    - `task_specialist_clone_guarded_expert_boost_default_merge`
    - `task_specialist_clone_guarded_expert_merge_default_merge`
    - against `guarded_expert`, `two_stage`, `task_ranking`, and `signature_selector`
  - mechanism:
    - keep the hard sparse branch and embedded signature branch fixed
    - remove the learned default-region head
    - replace it with either a hard `plasticity_boost` default or a hard `interference_control` default
  - result:
    - aggregate on `selector_full_map`:
      - `guarded_expert_selector`: mean `+4.3%` vs `adapt_only`, mean `+5.3%` vs fixed
      - `guarded_expert_boost_default`: mean `+3.9%` vs `adapt_only`, mean `+4.8%` vs fixed
      - `guarded_expert_merge_default`: mean `+2.0%` vs `adapt_only`, mean `+2.9%` vs fixed
      - `two_stage_selector`: mean `+4.4%` vs `adapt_only`, mean `+5.3%` vs fixed
    - key gates:
      - all guarded-expert variants still match `two_stage_selector` on
        - `selector_adversarial_gate`: `+0.0%` vs fixed
        - `selector_sparse_adversarial_gate`: `-0.8%` vs fixed
    - where the damage appears:
      - `boost_default` falls back to roughly the `signature_selector` frontier on non-gate suites
      - `merge_default` collapses badly on plasticity and retrieval-heavy suites
  - interpretation:
    - simplifying away the default-region learner is not free
    - preserving the sparse and embedded branches is enough to hold the adversarial gates, but not enough to hold the full aggregate frontier
    - the current sharp read is: the minimal effective selector structure is "hard sparse branch + hard embedded signature branch + learned default fallback"; anything simpler loses too much non-gate performance
