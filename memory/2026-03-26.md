# 2026-03-26

- Continued the repository-cleanup line instead of adding another local Phase 3 mechanism.
- Added `core/phase3_registry.py` as a code-consumable source of truth for:
  - canonical Phase 3 policy order
  - policy metadata (`family`, `forward_change`, `update_rule_change`, `memory_change`, `benchmark_intent`)
  - named policy sets like `digits_gap_core`
  - diagnostic suite families like `broader_digits`
- Updated `core/phase3_runner.py` to consume the registry instead of carrying another hard-coded full policy list.
- `run_phase3_ablations(...)` payload now includes:
  - `config.diagnostic_family`
  - `policy_metadata`
  - `diagnostic_suites`
- Updated `analysis/phase3_digits_gap_decomposition.py` to consume the Phase 3 registry instead of carrying its own separate policy tuple and suite family declaration.
- Extended smoke coverage so Phase 3 now asserts:
  - transfer suites resolve to `transfer_basics`
  - digits suites resolve to `broader_digits`
  - current-path boost is classified under `plasticity_boost`
- Verification:
  - `python tests/smoke_test.py`
  - `python analysis/phase3_digits_gap_decomposition.py`
  - `python analysis/generate_unified_report.py`
- Current read:
  - this round improved mechanism interpretability and reduced source-of-truth duplication without changing experimental results
  - the next cleanup target is no longer the runner list problem; it is the branch-heavy policy execution logic still embedded in `core/phase3_model.py`
- Continued the learner-core cleanup with a low-risk refactor inside `core/phase3_model.py`.
- Extracted `evolve()` pre-policy decision helpers:
  - `_source_idx_from_active(...)`
  - `_try_trend_limited_clone(...)`
  - `_try_margin_clone(...)`
  - `_passes_preclone_gate(...)`
- `evolve()` now reads more like:
  - early-stop checks
  - special-case trigger policies
  - shared source selection
  - clone-and-apply policy
- Verification:
  - `python tests/smoke_test.py`
- Current read:
  - behavior is unchanged under smoke coverage
  - the next remaining high-duplication block is the policy-application tail after `new_idx = self.add_unit(...)`
- Added archived-specialist quantization support to `Phase 3` via config:
  - `archived_specialist_quantization_bits`
  - applied only during forward passes of old specialists / memory units
  - deliberately used as a stress-test knob, not as a new policy name
- Added `analysis/phase3_archived_specialist_quantization_benchmark.py` and generated:
  - `PHASE3_ARCHIVED_SPECIALIST_QUANTIZATION_BENCHMARK.md`
  - `results/phase3_archived_specialist_quantization_benchmark.json`
- Strategy change during the run:
  - initial sweep `0 / 8 / 4 / 2` was too slow for the current loop budget
  - switched to a higher-information design:
    - reuse the existing unquantized `current_path_boost` results as baseline
    - run only `4-bit / 2-bit`
- Quantization benchmark result on broader digits:
  - unquantized baseline: mean `+11.9%` vs `adapt_only`, mean `-5.4%` vs fixed
  - `4-bit` archived specialists: mean `+13.3%` vs `adapt_only`, mean `-4.0%` vs fixed
  - `2-bit` archived specialists: mean `+13.1%` vs `adapt_only`, mean `-4.2%` vs fixed
- Interpretation:
  - this is a real positive signal, not just “it doesn’t break”
  - archived specialists seem to behave like coarse reusable memory; reducing their precision may actually regularize interference instead of harming reuse
  - TurboQuant-style thinking still is not the main mechanism, but compressed memory is now a credible side direction tightly connected to the main line
- Added `analysis/phase3_quantized_memory_path_benchmark.py` and generated:
  - `PHASE3_QUANTIZED_MEMORY_PATH_BENCHMARK.md`
  - `results/phase3_quantized_memory_path_benchmark.json`
- Quantized replacement result at `4-bit`:
  - `task_specialist_clone_current_path_boost_merge`: mean `+13.3%` vs `adapt_only`, mean `-4.0%` vs fixed, wins `3/3`
  - `task_specialist_clone_external_memory_boost_merge`: mean `+10.7%` vs `adapt_only`, mean `-6.6%` vs fixed, wins `0/3`
- Interpretation:
  - the positive quantization signal is not “all memory formats get better when compressed”
  - compressed archived specialists inside the main reuse structure remain strong
  - compressed external-memory rearrangement still loses, so the useful object is likely structured internal reuse, not generic low-bit memory banking
- Learner-core cleanup also continued:
  - added `_activate_task_specialist(...)`
  - added `_capture_anchor(...)`
  - added `_enable_current_path_boost_for_nonarchived(...)`
  - rewired several high-value policies to use those helpers
- Verification:
  - `python tests/smoke_test.py`
  - `python analysis/phase3_quantized_memory_path_benchmark.py`
  - `python analysis/generate_unified_report.py`
- Added compressed internal-memory budget support to `Phase 3` via config:
  - `archived_specialist_max_count`
  - applied only to archived specialist / memory-path candidates, capped by recency
- Added `analysis/phase3_internal_memory_budget_benchmark.py` and generated:
  - `PHASE3_INTERNAL_MEMORY_BUDGET_BENCHMARK.md`
  - `results/phase3_internal_memory_budget_benchmark.json`
- Found and fixed a real regression while adding the budget cap:
  - `dual_path_memory_outputs` had become a mixed list of arrays and tuples
  - this broke the Phase 3 ablation smoke matrix with a NumPy shape error
  - fixed by making the `memory_path_only` branch use the same tuple structure as the other memory-path branches
- Internal-memory budget result under `4-bit` archived-specialist quantization:
  - full budget: mean `+13.3%` vs `adapt_only`, mean `-4.0%` vs fixed
  - budget `2`: mean `+11.8%` vs `adapt_only`, mean `-5.5%` vs fixed
  - budget `1`: mean `+11.3%` vs `adapt_only`, mean `-6.1%` vs fixed
- Interpretation:
  - low-bit archived memory remains a real positive signal
  - but archive-count compression is not free; cutting down to `2` or `1` archived specialists consistently degrades the frontier
  - the useful current question is no longer “can we just keep one tiny archive?”, but “what compressed representation preserves the value of multiple archived specialists?”
- Verification:
  - `python tests/smoke_test.py`
  - `python analysis/phase3_internal_memory_budget_benchmark.py`
  - `python analysis/generate_unified_report.py`
- Added archive-selection support for capped archived specialists:
  - `archived_specialist_selection_mode` in `AblationConfig`
  - current modes: `recency` and `coverage`
- Added `analysis/phase3_internal_memory_selection_benchmark.py` and generated:
  - `PHASE3_INTERNAL_MEMORY_SELECTION_BENCHMARK.md`
  - `results/phase3_internal_memory_selection_benchmark.json`
- Selection result under `4-bit`, budget `2`:
  - `recency_2`: mean `+11.8%` vs `adapt_only`, mean `-5.5%` vs fixed
  - `coverage_2`: mean `+11.8%` vs `adapt_only`, mean `-5.6%` vs fixed
- Interpretation:
  - the budget-compression loss is not mainly caused by keeping the “wrong” archived specialists under a simple recency rule
  - a simple coverage selector does not recover the lost frontier
  - this pushes the memory direction away from cheap selector heuristics and toward richer memory representation/compression schemes
- Verification:
  - `python tests/smoke_test.py`
  - `python analysis/phase3_internal_memory_selection_benchmark.py`
  - `python analysis/generate_unified_report.py`
- Added minimal prototype-style archive compression support:
  - `archived_specialist_selection_mode='prototype'`
  - compresses capped archived entries into a small number of averaged forward prototypes
- Continued learner-core cleanup:
  - added `_set_archived_units_mode(...)`
  - used it to collapse repeated archived-unit state toggles in several boost / split policies
- Added `analysis/phase3_internal_memory_representation_benchmark.py` and generated:
  - `PHASE3_INTERNAL_MEMORY_REPRESENTATION_BENCHMARK.md`
  - `results/phase3_internal_memory_representation_benchmark.json`
- Representation result under `4-bit`, budget `2`:
  - `recency_2`: mean `+11.8%` vs `adapt_only`, mean `-5.5%` vs fixed
  - `prototype_2`: mean `+11.8%` vs `adapt_only`, mean `-5.6%` vs fixed
- Interpretation:
  - minimal output-space prototype compression does not recover the archive-count loss either
  - this further narrows the memory direction: cheap selector tweaks and cheap prototype compression both look saturated
  - if compressed memory is going to improve, it likely needs a richer representation than averaging archived outputs into a tiny budget
- Verification:
  - `python tests/smoke_test.py`
  - `python analysis/phase3_internal_memory_representation_benchmark.py`
  - `python analysis/generate_unified_report.py`
- Added weight-space archive prototype support:
  - `archived_specialist_selection_mode='weight_prototype'`
  - compresses capped archived specialists into averaged prototype units before forward execution
- Added `analysis/phase3_internal_memory_weight_prototype_benchmark.py` and generated:
  - `PHASE3_INTERNAL_MEMORY_WEIGHT_PROTOTYPE_BENCHMARK.md`
  - `results/phase3_internal_memory_weight_prototype_benchmark.json`
- Weight-space representation result under `4-bit`, budget `2`:
  - `recency_2`: mean `+11.8%` vs `adapt_only`, mean `-5.5%` vs fixed
  - `weight_prototype_2`: mean `+11.8%` vs `adapt_only`, mean `-5.6%` vs fixed
- Interpretation:
  - even a more structured lightweight prototype in weight space does not recover the archive-count loss
  - this pushes the quantized-memory story into a narrower box: low-bit precision is useful, but cheap low-budget compression tricks are not
- Verification:
  - `python tests/smoke_test.py`
  - `python analysis/phase3_internal_memory_weight_prototype_benchmark.py`
  - `python analysis/generate_unified_report.py`
- Added sample-conditioned key-value retrieval support:
  - `archived_specialist_selection_mode='key_value'`
  - retrieves low-budget archived specialists by cosine similarity between `abs(x)` and a simple weight-derived key
- Added `analysis/phase3_internal_memory_key_value_benchmark.py` and generated:
  - `PHASE3_INTERNAL_MEMORY_KEY_VALUE_BENCHMARK.md`
  - `results/phase3_internal_memory_key_value_benchmark.json`
- Key-value result under `4-bit`, budget `2`:
  - `recency_2`: mean `+11.8%` vs `adapt_only`, mean `-5.5%` vs fixed
  - `key_value_2`: mean `+11.8%` vs `adapt_only`, mean `-5.5%` vs fixed
- Interpretation:
  - even sample-conditioned retrieval fails to open a new frontier
  - across coverage, output prototype, weight prototype, and key-value retrieval, the same message repeats: low-bit archived memory is useful only when archive capacity remains broad enough
  - this compressed-memory micro-variant family now looks saturated
- Verification:
  - `python tests/smoke_test.py`
  - `python analysis/phase3_internal_memory_key_value_benchmark.py`
  - `python analysis/generate_unified_report.py`
- Continued learner-core cleanup without changing experiment semantics:
  - added `_activate_merged_specialist(...)`
  - rewired the repeated `task_specialist_clone_*merge` setup block from `limited_merge` through `distance_anchor_merge`
  - this removed a large repeated branch cluster inside `evolve()` while preserving outputs under smoke coverage
- Interpretation:
  - compressed-memory micro-variants now look saturated enough that the better use of engineering time is clearer learner-core structure
  - the next worthwhile engineering cut is the remaining boost/split tail in `evolve()`
- Verification:
  - `python tests/smoke_test.py`
  - `python analysis/generate_unified_report.py`
- Continued the same learner-core cleanup into the boost/split family:
  - added `_activate_boosted_specialist(...)`
  - rewired:
    - `task_specialist_clone_current_path_boost_merge`
    - `task_specialist_clone_decoupled_objective_boost_merge`
    - `task_specialist_clone_two_speed_boost_merge`
    - `task_specialist_clone_current_path_boost_retention_split_merge`
    - `task_specialist_clone_dual_path_boost_merge`
    - `task_specialist_clone_dual_path_decoupled_merge`
  - this removed another repeated policy-application block inside `evolve()` while keeping behavior unchanged under smoke coverage
- Interpretation:
  - the tail is now materially shorter and easier to reason about
  - remaining cleanup is narrower now: mostly the last few specialist variants and policy-specific suffix logic
- Verification:
  - `python tests/smoke_test.py`
  - `python analysis/generate_unified_report.py`
- Continued learner-core cleanup into the remaining trailing specialist variants:
  - rewired `task_specialist_clone_asymmetric_retention_merge`
  - rewired `task_specialist_clone_role_separation_merge`
  - rewired `task_specialist_clone_freeze_source`
  - these now reuse the same specialist-activation helpers instead of open-coded setup logic
- Interpretation:
  - `evolve()` tail is now materially flatter than before
  - remaining normalization work is smaller and more surgical now
- Verification:
  - `python tests/smoke_test.py`
  - `python analysis/generate_unified_report.py`
- Continued learner-core cleanup into the external-memory siblings:
  - added `_activate_external_memory_boosted_specialist(...)`
  - rewired:
    - `task_specialist_clone_external_memory_boost_merge`
    - `task_specialist_clone_external_memory_routed_merge`
  - this removed another repeated setup pair from `evolve()` while preserving behavior under smoke coverage
- Interpretation:
  - the remaining tail is now mostly truly policy-specific suffix logic rather than repeated setup scaffolding
- Verification:
  - `python tests/smoke_test.py`
  - `python analysis/generate_unified_report.py`
- Did one more final cleanup pass over the remaining tail:
  - the external-memory sibling pair now shares the same boosted-specialist activation helper
  - `evolve()` is now substantially flatter than before this cleanup sequence began
- Interpretation:
  - further gains from helper-level cleanup are now diminishing
  - the next engineering move should likely be a real policy-layer split, not more local helper extraction
- Verification:
  - `python tests/smoke_test.py`
  - `python analysis/generate_unified_report.py`
- Implemented the first real Phase 3 policy-layer split:
  - added `core/phase3_policies.py`
  - moved the clone-application tail out of `ReusePolicyNetwork.evolve()` into `apply_clone_policy(...)`
  - `core/phase3_model.py` now delegates post-clone policy setup instead of carrying that whole branch chain inline
- Interpretation:
  - this is an engineering-only change; no mechanism semantics changed
  - the important gain is that learner core and policy application are now separated enough to inspect independently
  - the next cleanup target is no longer "extract more helpers", but "normalize the new policy layer and shrink remaining policy-specific suffix logic"
- Verification:
  - `python tests/smoke_test.py`
  - `python analysis/generate_unified_report.py`
- Continued the same policy-layer split one step earlier in the `evolve()` flow:
  - added `resolve_max_units(...)` to `core/phase3_policies.py`
  - added `maybe_handle_special_evolve_case(...)` to `core/phase3_policies.py`
  - `ReusePolicyNetwork.evolve()` now delegates both special-case preclone dispatch and clone-application setup into the policy layer
- Interpretation:
  - `evolve()` is now much closer to a fixed skeleton instead of a policy switchboard
  - behavior is still unchanged under smoke coverage
  - the next cleanup step should focus on policy-layer normalization, not more ad hoc branch extraction
- Verification:
  - `python tests/smoke_test.py`
  - `python analysis/generate_unified_report.py`
- Normalized `core/phase3_policies.py` itself:
  - replaced the long internal `if policy == ...` chain with explicit handler functions and dispatch maps
- Re-entered the main research loop after the policy-layer cleanup and targeted the open selector gap in the new regime-switch story.
- Confirmed the current state first:
  - hardcoded `task_specialist_clone_regime_switch_merge` is the strongest policy on the formal stress-map family
  - simple scalar `task_specialist_clone_diagnostic_selector_merge` collapses back to the `current_path_boost` frontier
  - the next minimal high-value test should therefore add richer regime signals, not more local hybrid gates
- Added a richer online task fingerprint to `ReusePolicyNetwork`:
  - `task_stat_sums` now also tracks `input_abs_mean` and `input_nonnegative_ratio`
  - new config knobs:
    - `selector_input_nonnegative_threshold`
    - `selector_input_abs_mean_threshold`
  - new helper:
    - `_task_fingerprint_prefers_interference_control()`
- Added new policy:
  - `task_specialist_clone_fingerprint_selector_merge`
  - implementation: choose `limited_merge` vs `current_path_boost` by task input fingerprint rather than `task_suite` name or scalar conflict/confidence thresholds
- Added `analysis/phase3_fingerprint_selector_benchmark.py` and generated:
  - `PHASE3_FINGERPRINT_SELECTOR_BENCHMARK.md`
  - `results/canonical/phase3_fingerprint_selector_benchmark.json`
- Fingerprint-selector result on the formal stress-map family:
  - `task_specialist_clone_fingerprint_selector_merge` matches `task_specialist_clone_regime_switch_merge` exactly
  - mean `+7.5%` vs `adapt_only`
  - mean `+3.6%` vs fixed
  - the old `task_specialist_clone_diagnostic_selector_merge` remains at the weaker `current_path_boost` frontier
- Interpretation:
  - the gap was not “non-hardcoded selectors cannot work”
  - the gap was “scalar conflict/confidence stats are too weak; explicit task fingerprint is rich enough”
  - regime selection is now no longer only a benchmark-hardcoded story; it can already be recovered from online task-distribution signals
- Updated canonical/report chain:
  - `analysis/generate_unified_report.py` now includes `phase3_fingerprint_selector_benchmark.json`
  - `UNIFIED_REPORT.md` now states that richer task fingerprints recover the regime-switch frontier while simple scalar diagnostics do not
  - `HANDOFF.md` now tracks `task_specialist_clone_fingerprint_selector_merge` as the leading less-hardcoded formal stress-map selector
- Verification:
  - `python tests/smoke_test.py`
  - `python analysis/phase3_fingerprint_selector_benchmark.py`
- Ran one more selector-generalization probe by adding `feature_shift_nonnegative` to `core/phase3_common.py`:
  - intent: create an interference-style suite with digits-like nonnegative inputs to see if the new fingerprint selector was just keying on superficial input statistics
  - benchmark script: `analysis/phase3_selector_generalization_benchmark.py`
  - outputs:
    - `PHASE3_SELECTOR_GENERALIZATION_BENCHMARK.md`
    - `results/canonical/phase3_selector_generalization_benchmark.json`
- Result:
  - the new suite was not diagnostic enough
  - `limited_merge`, `current_path_boost`, `regime_switch`, `diagnostic_selector`, and `fingerprint_selector` all clustered around the same frontier
  - aggregate:
    - `task_specialist_clone_limited_merge`: `+12.2%` vs fixed
    - `task_specialist_clone_current_path_boost_merge`: `+12.3%` vs fixed
    - `task_specialist_clone_regime_switch_merge`: `+12.2%` vs fixed
    - `task_specialist_clone_diagnostic_selector_merge`: `+12.3%` vs fixed
    - `task_specialist_clone_fingerprint_selector_merge`: `+12.3%` vs fixed
- Interpretation:
  - this benchmark does not separate plasticity/interference regimes cleanly enough to test selector generalization
  - nonnegative-input transformation alone is a weak adversarial stress
  - this is a useful negative benchmark-design result: the next selector-generalization benchmark needs stronger regime ambiguity, not just surface input-stat shifts
- Verification:
  - `python analysis/phase3_selector_generalization_benchmark.py`
  - `python tests/smoke_test.py`
- Designed a stronger adversarial selector benchmark after the weak `feature_shift_nonnegative` result:
  - added `feature_shift_embedded` to `core/phase3_common.py`
  - it preserves interference-style task logic but encodes inputs into a 64-dim nonnegative, digits-like representation
  - goal: test whether `task_specialist_clone_fingerprint_selector_merge` is using causal regime structure or superficial input statistics
- Added `analysis/phase3_selector_adversarial_benchmark.py` and generated:
  - `PHASE3_SELECTOR_ADVERSARIAL_BENCHMARK.md`
  - `results/canonical/phase3_selector_adversarial_benchmark.json`
- Adversarial selector result:
  - `task_specialist_clone_regime_switch_merge`: gain vs fixed `-0.6%`
  - `task_specialist_clone_diagnostic_selector_merge`: gain vs fixed `-1.2%`
  - `task_specialist_clone_fingerprint_selector_merge`: gain vs fixed `-1.2%`
  - `task_specialist_clone_limited_merge`: gain vs fixed `-0.6%`
  - `task_specialist_clone_current_path_boost_merge`: gain vs fixed `-1.2%`
- Interpretation:
  - this is a useful negative result
  - the fingerprint selector does not yet survive adversarial regime ambiguity; it collapses to the same frontier as `current_path_boost`
  - hardcoded regime switching still behaves correctly, so the problem is no longer “can regime switching help?” but “how do we infer the right regime without depending on superficial family markers?”
  - the next selector probably needs learned or outcome-grounded regime inference, not more hand-tuned fingerprint thresholds
- Updated report chain:
  - `analysis/generate_unified_report.py` now includes `phase3_selector_adversarial_benchmark.json`
  - `UNIFIED_REPORT.md` now states the refined selector story:
    - hardcoded regime switch works
    - scalar diagnostics fail
    - simple fingerprints work on the current family
    - adversarial ambiguity still breaks them
- Verification:
  - `python analysis/phase3_selector_adversarial_benchmark.py`
  - `python tests/smoke_test.py`
  - split the file into:
    - `SPECIAL_EVOLVE_HANDLERS`
    - `CLONE_POLICY_HANDLERS`
    - small per-policy handler functions
- Interpretation:
  - this is still an engineering-only change with behavior preserved under smoke coverage
  - the meaningful gain is that Phase 3 policy semantics are now inspectable as named handler entries instead of scattered conditionals
  - the next useful cleanup step would be stronger family-level grouping or registry-backed dispatch, not another ad hoc flattening pass
- Verification:
  - `python tests/smoke_test.py`
  - `python analysis/generate_unified_report.py`
- Continued policy-layer normalization by grouping handlers into explicit families:
  - added family-level handler maps in `core/phase3_policies.py`
    - `CLONE_REUSE_POLICY_HANDLERS`
    - `SPECIALIST_REUSE_POLICY_HANDLERS`
    - `ROUTING_GATING_POLICY_HANDLERS`
    - `RETENTION_CONTROL_POLICY_HANDLERS`
    - `PLASTICITY_BOOST_POLICY_HANDLERS`
    - `OPTIMIZER_SPLIT_POLICY_HANDLERS`
    - `MEMORY_SPLIT_POLICY_HANDLERS`
    - `GROWTH_CONTROL_POLICY_HANDLERS`
  - `CLONE_POLICY_HANDLERS` is now composed from those family maps instead of being the only structure
- Interpretation:
  - behavior is still unchanged under smoke coverage
  - this makes the code-layer organization look much closer to the scientific mechanism families already used in the Phase 3 registry/reporting
  - the next cleanup step should be either registry-backed dispatch or shared family scaffolds, not more flat handler shuffling
- Verification:
  - `python tests/smoke_test.py`
  - `python analysis/generate_unified_report.py`
- Continued the same cleanup by reducing policy-family double maintenance:
  - `core/phase3_policies.py` now imports `get_phase3_policy_spec(...)` from `core/phase3_registry.py`
  - family-level handler maps are now built from registry family metadata instead of being hand-assigned in two places
- Interpretation:
  - behavior is still unchanged under smoke coverage
  - this is a real engineering-quality improvement: policy family structure in the execution layer is now partially derived from the same source of truth used by reporting/analysis
  - the next meaningful step would be stronger registry-backed dispatch, not more family reshuffling
- Verification:
  - `python tests/smoke_test.py`
  - `python analysis/generate_unified_report.py`
- Repaired the canonical report chain so the scientific source of truth matches the actual current main line:
  - updated `analysis/generate_unified_report.py`
  - `UNIFIED_REPORT.md` now treats `task_specialist_clone_current_path_boost_merge` as the broader-digits leading mechanism
  - `task_specialist_clone_distill_merge` is now described as the narrower base-`digits_pairs` tuned point, not the broader benchmark leader
- Interpretation:
  - this fixes a real credibility problem; report and handoff no longer disagree about the main Phase 3 boundary conclusion
  - the next repo-quality item should be structural consistency testing for Phase 3, not more report wording churn
- Verification:
  - `python analysis/generate_unified_report.py`
- Added Phase 3 structural consistency coverage to `tests/smoke_test.py`:
  - registry policy names vs policy-layer handlers
  - family grouping in `core/phase3_policies.py` vs registry family metadata
  - canonical `UNIFIED_REPORT.md` wording for the current broader-digits main line
- During this work, the new tests exposed two real boundary distinctions that are now encoded explicitly:
  - `best_clone` and `low_tension_clone` use the default clone skeleton and do not need post-clone handlers
  - `benefit_limited_clone` is both a preclone-gated policy and a post-clone-handler policy
- Interpretation:
  - this materially improves repo trustworthiness; policy metadata, execution layer, and canonical report now have smoke-level protection against drift
  - the next cleanup target should be canonical-results/result-layer structure, not more ad hoc consistency checks
- Verification:
  - `python tests/smoke_test.py`
- Added an explicit canonical-results manifest to the report chain:
  - `analysis/generate_unified_report.py` now defines `CANONICAL_RESULT_FILES`
  - report building now loads canonical inputs through that manifest instead of a scattered run of ad hoc `load_json_result(...)` calls
  - `REPORT_STATUS.md` now states that this manifest defines the canonical evidence chain
  - `tests/smoke_test.py` now checks that every canonical file in the manifest exists
- Interpretation:
  - this does not yet fully separate `results/` into canonical/sweep/smoke folders, but it does create a real source of truth for what counts as canonical input
  - the next worthwhile repo-quality step would be result-directory stratification, not more implicit result loading cleanup
- Verification:
  - `python analysis/generate_unified_report.py`
  - `python tests/smoke_test.py`
- Started actual results-directory stratification instead of only documenting it:
  - added `resolve_canonical_results_path(...)` and `resolve_smoke_results_path(...)` to `core/runtime.py`
  - `analysis/generate_unified_report.py` now saves `unified_summary.json` to `results/canonical/`
  - smoke tests now write their artifacts to `results/smoke/`
  - canonical loaders still fall back to legacy root-level files for compatibility
- Interpretation:
  - this is a practical first cut, not full migration
  - the repo now has real directory-level separation for new canonical/smoke outputs, while older result files remain readable
  - the next result-layer step, if we keep going, should be sweeping more canonical producers onto `results/canonical/`, not more naming-only cleanup
- Verification:
  - `python analysis/generate_unified_report.py`
  - `python tests/smoke_test.py`
- Continued the result-layer cleanup into exploratory scripts:
  - added `resolve_exploratory_results_path(...)` to `core/runtime.py`
  - rewired old standalone scripts away from hard-coded `F:/skill/sel-lab/results/...`
    - `core/convergence_analysis.py`
    - `core/convergence_quick.py`
    - `core/data_augmentation.py`
    - `core/learning_rate_schedule.py`
    - `core/phase4_quick.py`
    - `core/phase4_mnist_full.py`
    - `core/phase_d_scale.py`
    - `core/structural_evolution_analysis.py`
  - those scripts now save under `results/exploratory/`
  - while doing this, found and fixed a real compatibility issue: standalone `python core\\...py` entrypoints cannot safely import `from core.runtime ...` because of an external `core` package collision; for these scripts the stable direct-run import is `from runtime ...`
- Interpretation:
  - this is a real repo-quality improvement, not just naming cleanup
  - old quick scripts no longer write outside the structured results layers, and they still work as direct entrypoints
  - root-level historical copies still exist, but new runs now land in `results/exploratory/`
- Verification:
  - `python core/convergence_quick.py`
  - `python core/learning_rate_schedule.py`
  - `python tests/smoke_test.py`
- Continued the same migration by cleaning canonical-first analysis loading:
  - added shared runtime helpers:
    - `resolve_existing_results_path(...)`
    - `load_json_result(...)`
  - `analysis/generate_unified_report.py` now uses the shared runtime helper instead of maintaining its own canonical/legacy fallback logic
  - `analysis/phase3_digits_gap_decomposition.py` now:
    - reads canonical-first with legacy fallback
    - writes its JSON to `results/canonical/`
  - `analysis/phase3_gap_analysis.py` now:
    - reads canonical-first with legacy fallback
    - writes its JSON to `results/exploratory/`
- Interpretation:
  - this reduces another source of result-chain drift: canonical vs legacy fallback is no longer duplicated ad hoc in each analysis script
  - the migration is still incomplete, but the central report path and one canonical Phase 3 diagnostic path are now aligned with the new result layers
- Verification:
  - `python analysis/phase3_gap_analysis.py`
  - `python analysis/phase3_digits_gap_decomposition.py`
  - `python analysis/generate_unified_report.py`
  - `python tests/smoke_test.py`
- Cleaned the noisier Phase 4 result path behavior:
  - `core/phase4_failure_analysis.py` no longer writes `results/phase4_failure_analysis.json` every time `analyze_phase4_failure(...)` is called
  - it now only saves when an explicit `result_filename` is provided; `main()` writes to `results/exploratory/phase4_failure_analysis.json`
  - `core/phase4_repair_experiment.py` now saves to `results/canonical/phase4_repair_sweep.json`
  - `core/phase4_expansion_sweep.py` now saves future runs to `results/canonical/phase4_expansion_sweep.json`
- Interpretation:
  - this removes a quiet source of root-level result pollution because smoke tests and inner sweep calls no longer emit analysis JSON by default
  - canonical Phase 4 sweep outputs are now aligned with the report manifest path convention
- Verification:
  - `python tests/smoke_test.py`
  - `python core/phase4_repair_experiment.py`
- Continued the bulk migration of Phase 3 analysis outputs by intent:
  - benchmark / transfer scripts are being retargeted to `results/canonical/`
  - sweep scripts are being retargeted to `results/exploratory/`
  - updated a large batch of analysis entrypoints accordingly, including:
    - canonical examples: `phase3_digits_transfer.py`, `phase3_digits_benchmark_transfer.py`, `phase3_current_path_boost_benchmark.py`, `phase3_source_aware_benchmark.py`
    - exploratory examples: `phase3_distill_sweep.py`, `phase3_current_path_boost_sweep.py`, `phase3_specialist_merge_sweep.py`
  - reran `analysis/phase3_digits_transfer.py`; it now saves to `results/canonical/phase3_digits_transfer.json`
  - reran `analysis/generate_unified_report.py` after that migration step
  - attempted to rerun `analysis/phase3_distill_sweep.py`, but it exceeded a 300s timeout and did not finish
- Interpretation:
  - the path migration itself is working
  - but some old exploratory sweeps are now clearly expensive enough that they should be treated as batch jobs, not as routine verification commands
  - this is useful repo-level information because it separates “cheap integrity checks” from “heavy exploratory reruns”
- Verification:
  - `python analysis/phase3_digits_transfer.py`
  - `python analysis/generate_unified_report.py`
  - `python tests/smoke_test.py`
- Added a first real Phase 3 diagnostic suite instead of only repeating broader-digits benchmarks:
  - extended `core/phase3_registry.py` with `PHASE3_DIAGNOSTIC_BENCHMARKS`
  - added `analysis/phase3_diagnostic_suite.py`
  - diagnostic stresses:
    - `plasticity_stress` -> `digits_pairs_noisy`
    - `interference_stress` -> `feature_shift`
    - `retrieval_ambiguity_stress` -> `digits_pairs_permuted`
  - canonical outputs:
    - `results/canonical/phase3_diagnostic_suite.json`
    - `PHASE3_DIAGNOSTIC_SUITE.md`
- New mechanism read from that suite:
  - `task_specialist_clone_current_path_boost_merge` wins `plasticity_stress`
  - `task_specialist_clone_limited_merge` wins `interference_stress`
  - `task_specialist_clone_current_path_boost_merge` wins `retrieval_ambiguity_stress`
  - aggregate family wins: `plasticity_boost` = 2, `specialist_reuse` = 1
  - this sharpens the current interpretation:
    - current-path plasticity is still the main broader-digits lever
    - but low-merge specialist reuse still looks more robust when the pressure is specifically interference rather than acquisition
- Reran a heavy canonical benchmark after the path migration:
  - `analysis/phase3_current_path_boost_benchmark.py` completed and rewrote `results/canonical/phase3_current_path_boost_benchmark.json`
  - leader remains `task_specialist_clone_current_path_boost_merge`
- Also found an operational boundary:
  - `analysis/phase3_digits_benchmark_transfer.py` still exceeded a 600s timeout
  - this script should be treated as a heavier batch rerun, not as a routine maintenance check
- Verification:
  - `python analysis/phase3_diagnostic_suite.py`
  - `python analysis/phase3_current_path_boost_benchmark.py`
  - `python analysis/generate_unified_report.py`
  - `python tests/smoke_test.py`
- Added a stronger interference-focused diagnostic family instead of relying on the single `feature_shift` read:
  - extended `core/phase3_common.py` with `feature_shift_noisy`
  - extended `core/phase3_registry.py` so `interference_stress` includes `feature_shift` and `feature_shift_noisy`
  - added `analysis/phase3_interference_diagnostic.py`
  - canonical outputs:
    - `results/canonical/phase3_interference_diagnostic.json`
    - `PHASE3_INTERFERENCE_DIAGNOSTIC.md`
- New mechanism read from the stronger interference family:
  - `task_specialist_clone_limited_merge` is the overall winner across `feature_shift` and `feature_shift_noisy`
  - `task_specialist_clone` is worse, so raw specialist reuse really does look interference-prone
  - `task_specialist_clone_current_path_boost_merge` is also worse here, so its broader-digits strength does not transfer to explicit interference-heavy settings
  - this sharpens the current Phase 3 picture:
    - `current_path_boost` is the stronger acquisition/plasticity mechanism
    - `limited_merge` is the stronger interference-control mechanism
- Verification:
  - `python analysis/phase3_interference_diagnostic.py`
  - `python tests/smoke_test.py`
- Tried a minimal hybrid mechanism instead of jumping to a larger redesign:
  - new policy: `task_specialist_clone_confidence_boost_merge`
  - implementation: current-path boost plus confidence-gated specialist merge
  - benchmark: `analysis/phase3_hybrid_boost_diagnostic.py`
  - outputs:
    - `results/canonical/phase3_hybrid_boost_diagnostic.json`
    - `PHASE3_HYBRID_BOOST_DIAGNOSTIC.md`
- Result:
  - aggregate winner on the mixed diagnostic trio is `task_specialist_clone_confidence_boost_merge`, but only by a tiny margin over plain `task_specialist_clone_current_path_boost_merge`
  - on `feature_shift_noisy`, `task_specialist_clone_limited_merge` still wins
  - on `digits_pairs_noisy` and `digits_pairs_permuted`, the hybrid is effectively tied with or only trivially above plain `current_path_boost`
- Interpretation:
  - this is not a leading-mechanism replacement
  - adding a small confidence gate on top of current-path boost is too weak to recover the interference robustness of `limited_merge`
  - the plasticity/interference split now looks structural rather than a missing one-line gate heuristic
- Verification:
  - `python analysis/phase3_hybrid_boost_diagnostic.py`
  - `python tests/smoke_test.py`
  - `python analysis/generate_unified_report.py`
- Tried a more structured dual-mode mechanism instead of another tiny gate:
  - new policy: `task_specialist_clone_mode_switch_boost_merge`
  - mechanism: keep `current_path_boost`, but downshift the boost toward a safer floor when specialist/base conflict rises
  - benchmark: `analysis/phase3_mode_switch_diagnostic.py`
  - outputs:
    - `results/canonical/phase3_mode_switch_diagnostic.json`
    - `PHASE3_MODE_SWITCH_DIAGNOSTIC.md`
- Result:
  - overall winner on the mixed diagnostic trio remains `task_specialist_clone_current_path_boost_merge`
  - `task_specialist_clone_mode_switch_boost_merge` is not useless: it partially recovers interference / forgetting pressure relative to plain `current_path_boost`
  - but it still loses the explicit interference-heavy suite to `task_specialist_clone_limited_merge`
  - and it gives back too much plasticity on `digits_pairs_noisy` / `digits_pairs_permuted`
  - aggregate:
    - `limited_merge`: mean `+3.9%` vs `adapt_only`, mean `-5.6%` vs fixed
    - `current_path_boost`: mean `+9.3%` vs `adapt_only`, mean `-0.2%` vs fixed
    - `mode_switch_boost`: mean `+7.0%` vs `adapt_only`, mean `-2.4%` vs fixed
- Interpretation:
  - this is a real mechanism signal, not a null run
  - conflict-aware downshifting produces the expected trade: less aggressive plasticity, less forgetting
  - but the trade is still too coarse to unify the plasticity and interference frontiers
  - next serious hybrid attempt, if any, should switch more than the current-path LR scalar
- Verification:
  - `python analysis/phase3_mode_switch_diagnostic.py`
  - `python tests/smoke_test.py`
- Tried a more explicit dual-mode update instead of only downshifting the current-path boost:
  - new policy: `task_specialist_clone_dual_mode_update_merge`
  - mechanism: under high conflict, current-path boost downshifts and archived specialists switch to retention-only updates
  - benchmark: `analysis/phase3_dual_mode_update_diagnostic.py`
  - outputs:
    - `results/canonical/phase3_dual_mode_update_diagnostic.json`
    - `PHASE3_DUAL_MODE_UPDATE_DIAGNOSTIC.md`
- Result:
  - aggregate is effectively the same frontier as `mode_switch_boost`
  - `current_path_boost`: mean `+9.3%` vs `adapt_only`, mean `-0.2%` vs fixed
  - `mode_switch_boost`: mean `+7.0%` vs `adapt_only`, mean `-2.4%` vs fixed
  - `dual_mode_update`: mean `+6.9%` vs `adapt_only`, mean `-2.6%` vs fixed
  - `limited_merge` still wins `feature_shift_noisy`
  - `current_path_boost` still wins `digits_pairs_noisy` and `digits_pairs_permuted`
- Interpretation:
  - this is another useful negative, not a null run
  - changing archived-specialist updates as well as the current-path boost still does not create a new frontier
  - the tradeoff now looks more structural than "we haven't switched enough local knobs yet"
  - next hybrid attempt, if any, should probably switch whole learning modes or benchmark routing conditions, not just per-step local update flags
- Verification:
  - `python analysis/phase3_dual_mode_update_diagnostic.py`
  - `python tests/smoke_test.py`
- Promoted the diagnostics from isolated scripts into a formal benchmark-family layer:
  - added `PHASE3_DIAGNOSTIC_FAMILIES` to `core/phase3_registry.py`
  - added stronger benchmark `interference_stress_strong` (`feature_shift_noisy`)
  - added `analysis/phase3_diagnostic_benchmark_family.py`
  - outputs:
    - `results/canonical/phase3_diagnostic_benchmark_family.json`
    - `PHASE3_DIAGNOSTIC_BENCHMARK_FAMILY.md`
- Formal stress-map result:
  - `plasticity_boost` wins `2` stresses
  - `specialist_reuse` wins `2` stresses
  - `mode_switch_boost` wins `0`
  - `dual_mode_update` wins `0`
  - retrieval side is still dominated by `current_path_boost`; stronger interference side is still dominated by `limited_merge`
- Interpretation:
  - this is a higher-density confirmation of the current read, not just a repackaging
  - the project now has a formal screening layer for future Phase 3 mechanisms
  - more importantly, it confirms at family level that recent mixed candidates still do not form a new leader class
- Verification:
  - `python analysis/phase3_diagnostic_benchmark_family.py`
  - `python tests/smoke_test.py`
- Added a higher-level upper-bound check instead of another local policy:
  - `analysis/phase3_regime_switch_oracle.py`
  - outputs:
    - `results/canonical/phase3_regime_switch_oracle.json`
    - `PHASE3_REGIME_SWITCH_ORACLE.md`
- Oracle result:
  - choose `current_path_boost` on `plasticity_stress`
  - choose `limited_merge` on `interference_stress`
  - choose `limited_merge` on `interference_stress_strong`
  - choose `current_path_boost` on `retrieval_ambiguity_stress`
  - aggregate:
    - oracle mean `+7.5%` vs `adapt_only`, mean `+3.6%` vs fixed
    - plain `current_path_boost` mean `+6.7%` vs `adapt_only`, mean `+2.8%` vs fixed
- Interpretation:
  - this is not a new mechanism result, but it is strong strategy information
  - higher-level regime switching has enough upside to justify real work
  - the project should now stop spending time on local hybrid tweaks and move toward explicit regime selection / regime-conditioned training
- Verification:
  - `python analysis/phase3_regime_switch_oracle.py`
- Implemented the first explicit high-level regime policy:
  - new policy: `task_specialist_clone_regime_switch_merge`
  - mechanism:
    - `digits_pairs*` -> use `current_path_boost` behavior
    - `feature_shift*` -> use `limited_merge` behavior
  - benchmark: `analysis/phase3_regime_switch_benchmark.py`
  - outputs:
    - `results/canonical/phase3_regime_switch_benchmark.json`
    - `PHASE3_REGIME_SWITCH_BENCHMARK.md`
- Result:
  - it matches the oracle direction on the current formal stress-map family
  - aggregate:
    - `regime_switch_merge`: mean `+7.5%` vs `adapt_only`, mean `+3.6%` vs fixed
    - `current_path_boost`: mean `+6.7%` vs `adapt_only`, mean `+2.8%` vs fixed
    - `limited_merge`: mean `+2.9%` vs `adapt_only`, mean `-1.0%` vs fixed
  - it wins all `4/4` stresses in the current explicit family because it routes to the right regime for each task family
- Interpretation:
  - this is the first strong positive after the long run of local hybrid negatives
  - the important story shift is not "one new micro-mechanism won"
  - it is "higher-level regime selection is the first thing that actually exploits the plasticity/interference split instead of averaging it away"
  - next work should make regime selection less benchmark-hardcoded and more learnable or diagnostic-driven
- Verification:
  - `python analysis/phase3_regime_switch_benchmark.py`
  - `python tests/smoke_test.py`
- Tried to de-hardcode regime switching with a simple task-wise diagnostic selector:
  - new policy: `task_specialist_clone_diagnostic_selector_merge`
  - mechanism: use current-task average conflict/confidence stats to choose `limited_merge` vs `current_path_boost`
  - benchmark: `analysis/phase3_diagnostic_selector_benchmark.py`
  - outputs:
    - `results/canonical/phase3_diagnostic_selector_benchmark.json`
    - `PHASE3_DIAGNOSTIC_SELECTOR_BENCHMARK.md`
- Result:
  - it matches `current_path_boost` on the digits stresses
  - but it fails to reproduce the interference wins of the hardcoded `regime_switch_merge`
  - aggregate effectively collapses back to the `current_path_boost` frontier instead of the stronger regime-switch frontier
- Interpretation:
  - this is a useful negative
  - the problem is no longer just "remove the hardcoded suite name"
  - the current scalar diagnostics are too weak to infer the right regime reliably
  - next selector work needs richer signals or a more explicit task/regime representation
- Verification:
  - `python analysis/phase3_diagnostic_selector_benchmark.py`
  - `python tests/smoke_test.py`
- Tried one richer handcrafted selector instead of another single-threshold rule:
  - new policy: `task_specialist_clone_signature_selector_merge`
  - mechanism: combine online conflict/confidence stats with input abs/nonnegative/zero-ratio signals and a conflict-peak check
  - new benchmark: `analysis/phase3_signature_selector_benchmark.py`
  - updated adversarial benchmark: `analysis/phase3_selector_adversarial_benchmark.py`
  - outputs:
    - `results/canonical/phase3_signature_selector_benchmark.json`
    - `PHASE3_SIGNATURE_SELECTOR_BENCHMARK.md`
    - refreshed `results/canonical/phase3_selector_adversarial_benchmark.json`
- Result:
  - on the formal stress-map family, the signature selector stays between the weak diagnostic selector and the stronger fingerprint/hardcoded frontier
    - `signature_selector`: mean `+6.9%` vs `adapt_only`, mean `+3.0%` vs fixed
    - `fingerprint_selector`: mean `+7.5%` vs `adapt_only`, mean `+3.6%` vs fixed
  - on `feature_shift_embedded`, the signature selector is the best non-hardcoded selector tested so far
    - `signature_selector`: mean `-0.5%` vs fixed
    - `diagnostic_selector` / `fingerprint_selector`: mean `-1.2%` vs fixed
    - hardcoded `regime_switch`: mean `-0.6%` vs fixed
- Interpretation:
  - this is a useful negative, not a main-line upgrade
  - richer handcrafted signatures can patch part of the adversarial ambiguity failure mode
  - but handcrafted selector logic is still too brittle to dominate both the formal family and the adversarial case at once
  - the next selector move should shift toward a learned or explicitly fit classifier over task-signature features rather than more hand-tuned boolean rules
- Verification:
  - `python analysis/phase3_signature_selector_benchmark.py`
  - `python analysis/phase3_selector_adversarial_benchmark.py`
- Tried a minimal fitted selector instead of more handwritten boolean logic:
  - new policy: `task_specialist_clone_fitted_selector_merge`
  - mechanism: fit a linear decision surface over the current task-signature features collected from the formal family:
    - `confidence`
    - `conflict_score`
    - `conflict_peak`
    - `input_abs_mean`
    - `input_nonnegative_ratio`
    - `input_zero_ratio`
  - benchmark: `analysis/phase3_fitted_selector_benchmark.py`
  - extra adversarial check:
    - `results/canonical/phase3_selector_adversarial_benchmark_fitted.json`
    - `PHASE3_SELECTOR_ADVERSARIAL_BENCHMARK_FITTED.md`
- Result:
  - on the formal stress-map family, the fitted selector matches the stronger fingerprint / hardcoded frontier
    - `fitted_selector`: mean `+7.5%` vs `adapt_only`, mean `+3.6%` vs fixed
  - on `feature_shift_embedded`, it still collapses to the weaker boost-side behavior
    - `fitted_selector`: mean `-1.2%` vs fixed
    - `signature_selector`: mean `-0.5%` vs fixed
- Interpretation:
  - this is another useful negative
  - the problem is no longer just “handwritten rule surfaces are bad”
  - even a fitted linear selector over the current feature set does not survive the adversarial regime-ambiguity gate
  - the next selector work should target better regime features, stronger supervision, or both
- Verification:
  - `python analysis/phase3_fitted_selector_benchmark.py`
  - fitted adversarial rerun saved to `results/canonical/phase3_selector_adversarial_benchmark_fitted.json`
