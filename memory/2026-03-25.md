# 2026-03-25

- Repository cleanup pass:
  - downgraded `core/stability_test.py` from a fake project-wide validator to an explicit `SC-4 only` perturbation script
  - removed hard-coded `SC-1/2/3/5 PASS` reporting and now save `stability_test_results.json` through the normal runtime path
  - rewrote `HANDOFF.md` into a current-state execution doc instead of a mixed historical log
  - split `core/phase3_ablation.py` into:
    - `core/phase3_model.py`
    - `core/phase3_runner.py`
    - thin compatibility wrapper `core/phase3_ablation.py`
  - verified compatibility with:
    - `python tests/smoke_test.py`
    - `python core/stability_test.py`
    - `python analysis/generate_unified_report.py`
  - current cleanup read:
    - Phase 3 behavior and imports survived the split
    - `stability_test.py` now reports the uncomfortable truth: current SC-4 perturbation check fails at larger sigma, instead of pretending the whole project passed
    - repo credibility is better, but policy architecture is still registry-by-branch and not yet normalized into explicit mechanism objects
- Continued the SEL-Lab reframe by implementing the `Phase 3` mechanism matrix as the main experimental line.
- Added `core/phase3_ablation.py` as a dedicated entrypoint for sequential-learning structure-reuse studies.
- Extended the ablation policies to include:
  - `adapt_only`
  - `random_clone`
  - `low_tension_clone`
  - `best_clone`
  - `best_clone_limited_adapt`
  - `best_clone_freeze_1`
  - `best_clone_freeze_2`
  - `best_clone_freeze_3`
- Re-ran `python core/phase3_ablation.py` after the freeze-window expansion.
- Current read of the ablation results:
  - `adapt_only` is still the strongest overall policy on the current synthetic sequential benchmark.
  - `low_tension_clone` currently behaves almost identically to `adapt_only`, suggesting the trigger often suppresses cloning.
  - `best_clone_limited_adapt` is the strongest true reuse variant and looks more promising than unconstrained cloning.
  - Freeze-window variants help less than limited adaptation under the current setup.
- Wired `results/phase3_ablation_results.json` into `analysis/generate_unified_report.py`.
- Re-generated `UNIFIED_REPORT.md` after the Phase 3 ablation run.
- Added smoke coverage for the Phase 3 ablation matrix so future sessions catch policy-regression drift.
- Added `core/phase2_ablation.py` so single-task experiments are now organized as a mechanism bench rather than only as separate win/loss scripts.
- Extended `core.phase2_common.EvolvingNetwork` with policy-aware evolution:
  - `adapt_only`
  - `random_growth`
  - `best_clone`
  - `low_tension_clone`
  - `best_clone_limited_adapt`
- Added smoke coverage for the Phase 2 mechanism bench.
- Fixed a regression where smoke tests were overwriting the canonical `phase2_ablation_results.json` and `phase3_ablation_results.json` files.
  Smoke artifacts now save to:
  - `results/smoke_phase2_ablation_results.json`
  - `results/smoke_phase3_ablation_results.json`
- README now exposes `python core/phase3_ablation.py` as a first-class entrypoint.
- README now also exposes `python core/phase2_ablation.py` as the mechanism-screening entrypoint.
- Re-ran the full Phase 2 / Phase 3 ablations after fixing the smoke-output regression.
- Added `best_clone_delayed_limited_adapt` to `Phase 3`:
  clone first, freeze for one task window, then adapt with a reduced learning rate.
- Full `Phase 3` rerun after that change shows:
  - `adapt_only` still leads overall at `+17.3%`
  - `best_clone_delayed_limited_adapt` is now the strongest true reuse policy at `+16.9%`
  - `best_clone_limited_adapt` still has lower forgetting, so the tradeoff is now gain vs forgetting
- Updated `UNIFIED_REPORT.md` so it now includes a dedicated `Phase 2 Mechanism Bench` section.
- Replaced stale static reports with archival notices:
  - `FINAL_REPORT.md`
  - `PROJECT_SUMMARY.md`
  - `REVIEW.md`
- Added `REPORT_STATUS.md` so the repo has an explicit "current vs archived" report map.
- Added `analysis/phase3_gap_analysis.py` so the repo can decompose why true-reuse policies still trail `adapt_only`.
- Added `analysis/phase3_specialist_merge_sweep.py` to sweep `task_specialist_clone_limited_merge` over merge-scale values.
- `task_specialist_clone_limited_merge` became the strongest mechanism result so far:
  - full ablation: `+18.3%` vs fixed, `1.3%` mean forgetting
  - this beats `adapt_only` on both average accuracy and forgetting
- specialist merge sweep results:
  - `0.10` gave the best average result in the tested set: `+18.4%` vs fixed, `1.7%` mean forgetting
  - `0.20` was close: `+18.3%` vs fixed, `1.3%` mean forgetting
  - the specialist-merge idea now looks like the main research finding, not just another ablation branch
- Remaining near-term work:
  - decide whether `adapt_only` should remain in the mechanism matrix as a baseline or become the new sequential-learning default
  - tighten `Phase 3` around the narrower question: what can beat `adapt_only` under sequential learning?
  - promote `task_specialist_clone_limited_merge` into the main report chain and rerun gap analysis against `adapt_only`
  - keep `Phase 2` as a failure-mode / mechanism-screening bench rather than a thesis-support line
- Updated `analysis/generate_unified_report.py` so `task_specialist_clone_limited_merge` is now treated as the main true-reuse challenger/result in `Phase 3`.
- Updated `analysis/phase3_gap_analysis.py` so the candidate set now centers on current meaningful reuse policies, including the specialist family.
- Re-generated:
  - `UNIFIED_REPORT.md`
  - `PHASE3_GAP_ANALYSIS.md`
  - `results/phase3_gap_analysis.json`
- Current canonical Phase 3 reading:
  - baseline: `adapt_only` at `+17.3%` vs fixed, `2.1%` mean forgetting
  - strongest reuse result: `task_specialist_clone_limited_merge` at `+18.3%` vs fixed, `1.3%` mean forgetting
  - delta vs `adapt_only`: `+1.0%` average accuracy and `+0.8%` forgetting delta
- Specialist-merge sweep remains the next boundary-testing direction:
  - refined local sweep now maps `0.05 / 0.10 / 0.15 / 0.20`
  - `0.05` best average point in refined sweep: `+18.4%` vs fixed, `+1.2%` vs `adapt_only`
  - `0.20` best forgetting point in refined sweep: `1.3%` mean forgetting, `+0.8%` forgetting delta vs `adapt_only`
- Added task-suite support to `Phase 3` through `Phase3Config.task_suite` and extended `create_task(...)` to support:
  - `default`
  - `rotated`
  - `feature_shift`
  - `noisy`
- Added `analysis/phase3_suite_transfer.py` and generated:
  - `PHASE3_SUITE_TRANSFER.md`
  - `results/phase3_suite_transfer.json`
- Current transfer read for `task_specialist_clone_limited_merge` at merge scale `0.05`:
  - `default`: `+1.2%` vs `adapt_only`
  - `rotated`: `+2.4%` vs `adapt_only`
  - `feature_shift`: `+2.2%` vs `adapt_only`
  - `noisy`: `+2.1%` vs `adapt_only`
- Strongest transfer findings:
  - best average suite: `rotated`
  - best forgetting delta: `feature_shift`
- Added smoke coverage for `Phase 3` task-suite support.
- Added `digits_pairs` as a more realistic `Phase 3` task suite using sequential digits binary pairs:
  - `(0,1) -> (2,3) -> (4,5) -> (6,7)`
- Added `analysis/phase3_digits_transfer.py` and generated:
  - `PHASE3_DIGITS_TRANSFER.md`
  - `results/phase3_digits_transfer.json`
- Current `digits_pairs` result at merge scale `0.05`:
  - `adapt_only`: avg `61.9%`, `-16.8%` vs fixed, forgetting `0.2%`
  - `task_specialist_clone_limited_merge`: avg `66.5%`, `+4.5%` vs `adapt_only`, forgetting delta `-4.8%`
- Interpretation:
  - specialist-merge still improves over `adapt_only` on a richer benchmark
  - but both sequential policies still lose to fixed structure on `digits_pairs`
  - this is the first strong boundary result showing the main mechanism is real but not yet sufficient
- Added `analysis/phase3_digits_merge_sweep.py` and generated:
  - `PHASE3_DIGITS_MERGE_SWEEP.md`
  - `results/phase3_digits_merge_sweep.json`
- Digits merge sweep result:
  - `0.02` best average point and closest to fixed: `+5.3%` vs `adapt_only`, still `-11.5%` vs fixed
  - `0.20` least-bad forgetting point: `-3.2%` forgetting delta vs `adapt_only`
  - conclusion: merge-scale tuning on `digits_pairs` is not enough; it only trades current-task gain against retention
- Added `task_specialist_clone_retention_merge` to the Phase 3 mechanism matrix.
- Retention-merge result:
  - synthetic smoke-scale run improved over plain specialist merge: `+15.8%` vs fixed, `1.7%` mean forgetting
  - `digits_pairs` comparison was nearly unchanged from plain specialist merge:
    avg `66.5%`, forgetting `5.0%`
- Interpretation:
  - task-distance-weighted merge is not enough to fix the richer benchmark boundary
  - future retention ideas should probably change routing/gating behavior more explicitly
- Added `AUTONOMY_PROTOCOL.md` so future autonomous work has an explicit loop:
  - strengthen `Phase 3` first
  - work in bounded experiment/report cycles
  - stop and report on strong positives, strong negatives, stalled loops, or thesis-level changes
- Current autonomous objective written into that protocol:
  improve the best available reuse mechanism on `digits_pairs` without losing its gain over `adapt_only`
- Updated `AUTONOMY_PROTOCOL.md` so the project goal constrains the question space, not the answer:
  - current leading mechanism may be replaced
  - stop conditions now include "leading mechanism changes"
- Added `FULL_AUTO_PROMPT.md` as the repo-specific `codex --full-auto` template.
- Added `task_specialist_clone_confidence_gate_merge` to `Phase 3`.
- Mechanism:
  - old task-specialist units keep their merge scale
  - but their contribution is down-weighted when the current non-specialist path is already confident
  - goal: preserve reuse on ambiguous samples while reducing unnecessary interference on `digits_pairs`
- Re-ran `python analysis/phase3_digits_transfer.py`.
- Current `digits_pairs` comparison:
  - `task_specialist_clone_limited_merge`: avg `66.5%`, forgetting `5.1%`
  - `task_specialist_clone_confidence_gate_merge`: avg `66.9%`, forgetting `4.7%`
  - delta vs old specialist-merge: `+0.4%` average accuracy and `-0.4%` forgetting
- Re-ran the full `python core/phase3_ablation.py`.
- Main matrix result for `task_specialist_clone_confidence_gate_merge`:
  - `+18.2%` vs fixed, `1.4%` mean forgetting
  - effectively tied with retention-merge, slightly behind `task_specialist_clone_limited_merge` (`+18.3%`, `1.3%`)
- Interpretation:
  - confidence-gating is a real positive on the richer boundary benchmark
  - but not enough to displace the current main synthetic leader
  - next useful work should probably combine confidence-gating with stronger role separation or routing, not just another scalar sweep
- Tried three more output-side or light-control variants on `digits_pairs`:
  - `task_specialist_clone_routed_merge`
  - `task_specialist_clone_soft_route_merge`
  - `task_specialist_clone_budgeted_merge`
- Result:
  all three underperformed `task_specialist_clone_confidence_gate_merge`; routing/weighting tweaks do not appear to be the main bottleneck.
- Added training-side retention variants:
  - `task_specialist_clone_anchor_merge`
  - `task_specialist_clone_distance_anchor_merge`
  - `task_specialist_clone_distill_merge`
- Read:
  - anchor-style constraints are directionally better than routing tweaks
  - `task_specialist_clone_distill_merge` is the first training-side variant to slightly beat `task_specialist_clone_confidence_gate_merge` on `digits_pairs`
- Added `analysis/phase3_distill_sweep.py` and generated:
  - `PHASE3_DISTILL_SWEEP.md`
  - `results/phase3_distill_sweep.json`
- Distillation sweep on `digits_pairs`:
  - tested strengths: `0.15 / 0.25 / 0.35 / 0.50 / 0.75`
  - best point in tested set: `0.50`
  - at `0.50`, `task_specialist_clone_distill_merge` reached:
    - avg `66.94%`
    - forgetting `4.67%`
    - delta vs confidence-gate: `+0.06%` avg and `+0.04%` forgetting improvement
- Re-ran full `python core/phase3_ablation.py`.
- Main matrix result:
  - `task_specialist_clone_distill_merge` is now the strongest average-accuracy policy at `+18.4%` vs fixed
  - `task_specialist_clone_limited_merge` remains slightly best on forgetting at `1.3%`
  - interpretation: the main leading mechanism changed from plain specialist-merge to distillation-regularized specialist reuse
- Added `analysis/phase3_distill_joint_sweep.py` and generated:
  - `PHASE3_DISTILL_JOINT_SWEEP.md`
  - `results/phase3_distill_joint_sweep.json`
- Joint sweep on `digits_pairs` over merge scale `0.02 / 0.05 / 0.10` and distill strength `0.25 / 0.50 / 0.75`:
  - strongest point in the tested grid is merge scale `0.02`
  - at merge `0.02`, distill strengths `0.25 / 0.50 / 0.75` all gave the same best reported average result
  - best observed result:
    - `task_specialist_clone_distill_merge` avg `67.3%`
    - `+5.4%` vs `adapt_only`
    - `-11.4%` vs fixed
    - `4.7%` mean forgetting
- Interpretation:
  - the next `digits_pairs` improvement came from reducing specialist merge strength while retaining distillation
  - current best boundary mechanism is no longer the original `0.05` specialist merge family
  - strongest current boundary recipe is effectively "low-merge specialist reuse + lightweight distillation"
- Added `analysis/phase3_distill_local_merge_sweep.py` and generated:
  - `PHASE3_DISTILL_LOCAL_MERGE_SWEEP.md`
  - `results/phase3_distill_local_merge_sweep.json`
- Local sweep around the new best point at fixed distill strength `0.50`:
  - merge `0.01`: avg `67.2%`, `-11.5%` vs fixed, forgetting `4.8%`
  - merge `0.02`: avg `67.3%`, `-11.4%` vs fixed, forgetting `4.7%`
  - merge `0.03`: avg `67.3%`, `-11.5%` vs fixed, forgetting `4.6%`
- Interpretation:
  - `0.02` remains the best average point and closest-to-fixed point
  - `0.03` is a small retention tradeoff point, not a true improvement
  - current `digits_pairs` tuned default is now:
    `task_specialist_clone_distill_merge` with merge scale `0.02` and distill strength `0.50`
- Added broader digits benchmark-family support to `Phase 3` task generation:
  - `digits_pairs_noisy`
  - `digits_pairs_permuted`
- Updated `analysis/phase3_digits_transfer.py` so tied best policies are recorded explicitly and `task_specialist_clone_distill_merge` remains the stable tie-broken winner on the base `digits_pairs` report.
- Added `analysis/phase3_digits_benchmark_transfer.py` and generated:
  - `PHASE3_DIGITS_BENCHMARK_TRANSFER.md`
  - `results/phase3_digits_benchmark_transfer.json`
- Broader digits-family result at shared tuned config (`merge=0.02`, `distill=0.50`):
  - `task_specialist_clone_limited_merge`: mean `+5.0%` vs `adapt_only`, mean `-3.8%` forgetting delta, mean `-12.3%` vs fixed, wins `2/3` suites
  - `task_specialist_clone_confidence_gate_merge`: mean `+4.9%` vs `adapt_only`, wins `0/3`
  - `task_specialist_clone_distill_merge`: mean `+4.9%` vs `adapt_only`, wins `1/3`
- Interpretation:
  - low-merge distillation still defines the best base `digits_pairs` point
  - but it does not remain the most robust reuse mechanism once the digits benchmark is broadened
  - `task_specialist_clone_limited_merge` is now the broader-digits robustness baseline
- Added `task_specialist_clone_role_separation_merge` to `Phase 3`.
- Mechanism:
  specialists update only on their own task, then stay read-only as archived memories on later tasks.
- Added `analysis/phase3_role_separation_benchmark.py` and generated:
  - `PHASE3_ROLE_SEPARATION_BENCHMARK.md`
  - `results/phase3_role_separation_benchmark.json`
- Role-separation benchmark result:
  - `task_specialist_clone_role_separation_merge`: mean `+4.9%` vs `adapt_only`, mean `-3.9%` forgetting delta, mean `-12.4%` vs fixed, suite wins `0/3`
  - it does not beat `task_specialist_clone_limited_merge` on any broader digits suite
- Interpretation:
  - the simplest archive-style role separation is not enough to improve the current robustness baseline
  - the remaining problem is likely source-aware interaction, not just preventing old specialists from updating
- Added `task_specialist_clone_source_aware_merge` to `Phase 3`.
- Mechanism:
  specialist merge is scaled by both base-path uncertainty and specialist/base disagreement magnitude.
- Added `analysis/phase3_source_aware_benchmark.py` and generated:
  - `PHASE3_SOURCE_AWARE_BENCHMARK.md`
  - `results/phase3_source_aware_benchmark.json`
- Source-aware benchmark result:
  - `task_specialist_clone_source_aware_merge`: mean `+4.9%` vs `adapt_only`, mean `-3.9%` forgetting delta, mean `-12.3%` vs fixed, suite wins `0/3`
  - it ties the base `digits_pairs` point but still does not beat `task_specialist_clone_limited_merge` on the broader family
- Interpretation:
  - simple source-aware output gating is not enough either
  - after both role-separation and source-aware gating negatives, the next promising direction should be asymmetric learning dynamics rather than more merge-time gating tweaks
- Added `task_specialist_clone_asymmetric_retention_merge` to `Phase 3`.
- Mechanism:
  on later tasks, old specialists no longer follow the full task error and only receive a weak retention-style pull toward their stored snapshot.
- Added `analysis/phase3_asymmetric_retention_benchmark.py` and generated:
  - `PHASE3_ASYMMETRIC_RETENTION_BENCHMARK.md`
  - `results/phase3_asymmetric_retention_benchmark.json`
- Asymmetric-retention benchmark result:
  - `task_specialist_clone_asymmetric_retention_merge`: mean `+4.9%` vs `adapt_only`, mean `-3.8%` forgetting delta, mean `-12.5%` vs fixed, suite wins `0/3`
  - it does not beat `task_specialist_clone_limited_merge` on any broader digits suite
- Interpretation:
  - weak asymmetric updates are not enough either
  - after three consecutive negative mechanism variants around the same low-merge specialist family, this local search direction now looks saturated
- Added `analysis/phase3_digits_gap_decomposition.py` and generated:
  - `PHASE3_DIGITS_GAP_DECOMPOSITION.md`
  - `results/phase3_digits_gap_decomposition.json`
- Fixed-gap decomposition result on broader digits:
  - for `adapt_only` and every leading reuse candidate, the dominant remaining gap vs fixed is `current-task gap`, not `prior-task gap`
  - `task_specialist_clone_limited_merge` remains the best broader-digits reuse baseline, but its mean current gap vs fixed (`-27.8%`) is still much larger than its mean prior gap (`-7.1%`)
- Interpretation:
  - recent retention/gating/role-separation tweaks have mostly been attacking a secondary bottleneck
  - the next major direction should target current-task plasticity/capacity rather than only retention
- Added `task_specialist_clone_current_path_boost_merge` to `Phase 3`.
- Mechanism:
  preserve low-merge specialist memory reuse, but boost learning rate on the current active path so current-task plasticity is no longer bottlenecked by the same conservative dynamics used for archival specialists.
- Added `analysis/phase3_current_path_boost_benchmark.py` and generated:
  - `PHASE3_CURRENT_PATH_BOOST_BENCHMARK.md`
  - `results/phase3_current_path_boost_benchmark.json`
- Current-path boost benchmark result:
  - `task_specialist_clone_current_path_boost_merge`: mean `+10.8%` vs `adapt_only`, mean current-task delta `+28.6%`, mean `-8.5%` forgetting delta vs `adapt_only`, mean `-6.5%` vs fixed, wins `3/3` broader digits suites
  - on `digits_pairs_permuted` it nearly closes the fixed gap: `-0.5%`
- Interpretation:
  - this is the first strong positive after several negative retention/gating variants
  - the fixed-gap decomposition was directionally correct: improving current-task plasticity matters far more than more retention tweaks
  - the leading broader-digits mechanism has changed from `task_specialist_clone_limited_merge` to `task_specialist_clone_current_path_boost_merge`
- Added `analysis/phase3_current_path_boost_sweep.py` and generated:
  - `PHASE3_CURRENT_PATH_BOOST_SWEEP.md`
  - `results/phase3_current_path_boost_sweep.json`
- Current-path boost sweep result:
  - tested `1.25 / 1.50 / 1.75 / 2.00`
  - best point in the tested band is `2.00`
  - mean avg delta vs `adapt_only` improves monotonically across the tested band:
    `+7.2% -> +9.1% -> +10.8% -> +11.9%`
  - mean fixed-gap improves monotonically too:
    `-10.1% -> -8.3% -> -6.5% -> -5.4%`
  - at `2.00`, `digits_pairs_permuted` reaches `+0.5%` vs fixed
- Interpretation:
  - the current-path boost gain is not a brittle one-off setting
  - stronger current-task plasticity in this local band continues to help, though forgetting also rises
  - the tuned current boundary default is now `task_specialist_clone_current_path_boost_merge` with boost `2.00`
- Added `analysis/phase3_current_path_boost_upper_sweep.py` and generated:
  - `PHASE3_CURRENT_PATH_BOOST_UPPER_SWEEP.md`
  - `results/phase3_current_path_boost_upper_sweep.json`
- Current-path boost upper sweep result:
  - tested `2.00 / 2.25 / 2.50`
  - best point in the tested upper band is `2.50`
  - mean avg delta vs `adapt_only` continues improving:
    `+11.9% -> +12.8% -> +13.6%`
  - mean fixed-gap also improves:
    `-5.4% -> -4.2% -> -3.9%`
  - `digits_pairs_permuted` improves from `+0.5%` vs fixed at `2.00` to `+1.1%` at `2.50`
- Interpretation:
  - the current-path plasticity story strengthens further in the tested upper band
  - forgetting continues to rise, so this is now a sharper plasticity/retention frontier rather than a free win
  - the tuned current boundary default should move from `2.00` to `2.50`
- Added `task_specialist_clone_dual_path_boost_merge` to `Phase 3`.
- Mechanism:
  keep `current_path_boost=2.50`, but separate archived specialists into a memory-only forward path while non-archived units keep the boosted current-task update route.
- Added `analysis/phase3_dual_path_boost_benchmark.py` and generated:
  - `PHASE3_DUAL_PATH_BOOST_BENCHMARK.md`
  - `results/phase3_dual_path_boost_benchmark.json`
- Dual-path boost benchmark result:
  - `task_specialist_clone_current_path_boost_merge`: mean `+13.2%` vs `adapt_only`, mean current-task delta `+34.0%`, mean `-12.8%` forgetting delta vs `adapt_only`, mean `-4.1%` vs fixed, suite wins `3/3`
  - `task_specialist_clone_dual_path_boost_merge`: mean `+10.9%` vs `adapt_only`, mean current-task delta `+30.7%`, mean `-13.5%` forgetting delta vs `adapt_only`, mean `-6.4%` vs fixed, suite wins `0/3`
  - it loses to single-path `current_path_boost_merge` on all broader digits suites
- Interpretation:
  - a minimal explicit current-path/memory-path split is not enough to improve the plasticity/retention frontier
  - the stronger current-task channel is still the key positive lever, but this first split implementation mostly adds cost without adding robustness
  - the next structural attempt should be larger than another local dual-path tweak inside the same low-merge specialist family
- Added `task_specialist_clone_dual_path_decoupled_merge` to `Phase 3`.
- Mechanism:
  keep the dual-path forward split, but make task loss train only the current path; archived memory specialists remain inference-only plus retention-style maintenance.
- Added `analysis/phase3_dual_path_decoupled_benchmark.py` and generated:
  - `PHASE3_DUAL_PATH_DECOUPLED_BENCHMARK.md`
  - `results/phase3_dual_path_decoupled_benchmark.json`
- Dual-path decoupled benchmark result:
  - `task_specialist_clone_current_path_boost_merge`: mean `+13.2%` vs `adapt_only`, mean current-task delta `+34.0%`, mean `-12.8%` forgetting delta vs `adapt_only`, mean `-4.1%` vs fixed, suite wins `3/3`
  - `task_specialist_clone_dual_path_decoupled_merge`: mean `+13.2%` vs `adapt_only`, mean current-task delta `+34.5%`, mean `-13.3%` forgetting delta vs `adapt_only`, mean `-4.1%` vs fixed, suite wins `1/3`
  - it effectively ties the single-path `current_path_boost_merge` on average, but does not improve the frontier and does not become a new leader
- Interpretation:
  - removing memory-path contribution from task-loss training is not sufficient to create a better robustness/plasticity tradeoff
  - this narrows the hypothesis further: the problem is not just forward coupling or loss coupling inside one shared structure
  - the next structural attempt should target a genuinely separate memory network or stronger optimizer-level isolation
- Added `task_specialist_clone_external_memory_boost_merge` to `Phase 3`.
- Mechanism:
  archive prior specialists into a separate inference-only external memory bank while the current trainable network continues with boosted current-path learning.
- Added `analysis/phase3_external_memory_boost_benchmark.py` and generated:
  - `PHASE3_EXTERNAL_MEMORY_BOOST_BENCHMARK.md`
  - `results/phase3_external_memory_boost_benchmark.json`
- External-memory boost benchmark result:
  - `task_specialist_clone_current_path_boost_merge`: mean `+13.2%` vs `adapt_only`, mean current-task delta `+34.0%`, mean `-12.8%` forgetting delta vs `adapt_only`, mean `-4.1%` vs fixed, suite wins `3/3`
  - `task_specialist_clone_external_memory_boost_merge`: mean `+9.4%` vs `adapt_only`, mean current-task delta `+29.7%`, mean `-14.0%` forgetting delta vs `adapt_only`, mean `-10.4%` vs fixed, suite wins `0/3`
  - it improves strongly over `adapt_only`, but still loses clearly to the single-network `current_path_boost` baseline
- Interpretation:
  - a truly separate external memory bank is still not enough to beat the current-path plasticity baseline
  - moving old specialists out of the trainable pool slightly recovers retention pressure, but the current-task cost is too large
  - current evidence now points away from more memory-split variants and toward stronger optimizer-level separation or different memory-query mechanisms
- Added `task_specialist_clone_external_memory_routed_merge` to `Phase 3`.
- Mechanism:
  keep the external memory bank, but replace average memory merge with routed retrieval so only the strongest archived memory contributes for a given sample.
- Added `analysis/phase3_external_memory_routed_benchmark.py` and generated:
  - `PHASE3_EXTERNAL_MEMORY_ROUTED_BENCHMARK.md`
  - `results/phase3_external_memory_routed_benchmark.json`
- External-memory routed benchmark result:
  - `task_specialist_clone_current_path_boost_merge`: mean `+13.2%` vs `adapt_only`, mean current-task delta `+34.0%`, mean `-12.8%` forgetting delta vs `adapt_only`, mean `-4.1%` vs fixed, suite wins `3/3`
  - `task_specialist_clone_external_memory_routed_merge`: mean `+10.7%` vs `adapt_only`, mean current-task delta `+30.1%`, mean `-13.5%` forgetting delta vs `adapt_only`, mean `-6.7%` vs fixed, suite wins `0/3`
  - it is essentially tied with plain external-memory merge and still clearly worse than the single-network `current_path_boost` baseline
- Interpretation:
  - a lightweight routed query over the external memory bank does not solve the problem either
  - this makes the boundary sharper: neither splitting memory out nor adding simple retrieval on top of it is enough
  - the next credible move should target optimizer-level separation or a more radical change in memory representation/query, not another local retrieval heuristic
- Added `task_specialist_clone_two_speed_boost_merge` to `Phase 3`.
- Mechanism:
  keep the single-network `current_path_boost` recipe, but explicitly slow updates on old specialists with an extra old-path LR multiplier while preserving the boosted current path.
- Added `analysis/phase3_two_speed_boost_benchmark.py` and generated:
  - `PHASE3_TWO_SPEED_BOOST_BENCHMARK.md`
  - `results/phase3_two_speed_boost_benchmark.json`
- Two-speed boost benchmark result:
  - `task_specialist_clone_current_path_boost_merge`: mean `+13.2%` vs `adapt_only`, mean current-task delta `+34.0%`, mean `-12.8%` forgetting delta vs `adapt_only`, mean `-4.1%` vs fixed, suite wins `3/3`
  - `task_specialist_clone_two_speed_boost_merge`: mean `+13.2%` vs `adapt_only`, mean current-task delta `+34.0%`, mean `-12.8%` forgetting delta vs `adapt_only`, mean `-4.1%` vs fixed, suite wins `0/3`
  - it is effectively identical to `current_path_boost_merge` on all broader digits suites
- Interpretation:
  - a trivial optimizer split via old-path LR scaling does not create a new mechanism
  - this suggests the current baseline already suppresses old-path learning enough that another scalar LR penalty is redundant
  - future optimizer-level work should be more structural than simple per-path LR scaling
- Added `task_specialist_clone_decoupled_objective_boost_merge` to `Phase 3`.
- Mechanism:
  keep the single-network `current_path_boost` recipe, but let old specialists stop following task loss and update only via anchor-consistency while the current path keeps the full task objective.
- Added `analysis/phase3_decoupled_objective_boost_benchmark.py` and generated:
  - `PHASE3_DECOUPLED_OBJECTIVE_BOOST_BENCHMARK.md`
  - `results/phase3_decoupled_objective_boost_benchmark.json`
- Decoupled-objective boost benchmark result:
  - `task_specialist_clone_current_path_boost_merge`: mean `+13.2%` vs `adapt_only`, mean current-task delta `+34.0%`, mean `-12.8%` forgetting delta vs `adapt_only`, mean `-4.1%` vs fixed, suite wins `3/3`
  - `task_specialist_clone_decoupled_objective_boost_merge`: mean `+13.2%` vs `adapt_only`, mean current-task delta `+34.0%`, mean `-12.7%` forgetting delta vs `adapt_only`, mean `-4.1%` vs fixed, suite wins `0/3`
  - it is effectively identical to `current_path_boost_merge` on the broader digits family
- Interpretation:
  - a simple objective split between current-task loss and old-path consistency does not create a new mechanism either
  - this suggests old specialists are already weakly coupled enough in the current baseline that local objective reassignment is redundant
  - future optimization work should be more structural than a local consistency-only reassignment
