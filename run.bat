@echo off

:loop
codex --full-auto "
你正在推进 SEL-Lab，一个持续实验系统。

【初始化阶段（仅第一轮执行）】
先读取：
- HANDOFF.md
- AUTONOMY_PROTOCOL.md
- UNIFIED_REPORT.md
- 最新 results 文件
- 如有需要再读 README.md

理解当前：
- 项目目标
- 当前主线（Phase 3 / digits_pairs）
- 已知结论
- 当前 leading mechanism

--------------------------------------------------

【核心研究问题（不可改变）】

forward-only structure reuse 在 continual learning 中：
- 何时有效？
- 哪些机制带来收益？
- 为什么有效或失败？

--------------------------------------------------

【当前研究策略（默认优先）】

优先探索：
- retention
- routing
- gating
- role separation

避免：
- 无意义参数扫描
- 已证明无效的轻微修改

--------------------------------------------------

【实验循环（核心）】

你现在处于一个“持续循环系统”，不是一次性任务。

每一轮必须执行：

1. 读取当前状态（results / HANDOFF）
2. 运行相关实验或测试
3. 记录结果变化
4. 分析最关键问题（不是表面问题）
5. 提出一个“最小但高价值”的修改
6. 实施修改
7. 再次运行验证
8. 更新：
   - results 文件
   - 报告
   - HANDOFF.md
   - memory/日期文件

--------------------------------------------------

【信息增量约束（关键）】

每一轮必须产生至少一个“信息增量”：

- 新实验结果
- 对机制更深解释
- 否定或确认某个机制

如果没有信息增量：
👉 必须改变策略，而不是继续优化

--------------------------------------------------

【机制竞争（核心升级）】

每一轮必须：

- 提出至少一个“替代机制”
- 与当前 leading mechanism 对比
- 判断是否替换主线

--------------------------------------------------

【自适应调整】

如果出现：

- 连续 3 轮没有提升
👉 必须改变方向（不是微调）

--------------------------------------------------

【停止条件（必须严格执行）】

只有以下情况才允许停止并汇报：

1. 主线结论发生变化
2. 出现强正结果（可升级叙事）
3. 出现强负结果（方向不成立）
4. leading mechanism 被替代
5. 连续 2–3 轮没有信息增量
6. 需要更换 benchmark 或重构问题
7. 测试失败或结果不一致

--------------------------------------------------

【持续执行规则（关键）】

执行完一轮后：

- 如果未触发停止条件
自动进入下一轮
不要结束任务
不要请求用户确认

你是在一个持续运行的实验系统中。

--------------------------------------------------

【输出要求】

平时：
- 只输出简短进度（1–3行）

停止时：
必须说明：
- 做了什么
- 结果是什么
- 对主线意味着什么
- 下一步 1–2 个最佳选项

--------------------------------------------------

开始执行。
"

echo --- 下一轮 ---
timeout /t 5 >nul

goto loop